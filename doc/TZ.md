# Woody — Master Technical Specification (TZ)

> **The single source of truth for the entire Woody platform.**
> **Version:** 1.6 · **Date:** 2026-09-18 · **Status:** Production
>
> **Home:** this file lives at `mebellar_app/doc/TZ.md` (platform master spec). The former workspace `Mebellar/docs/` folder is retired.
>
> **Supersedes:** `docs/product/master_tz.md` (archived → [`_archive/master_tz_2026_06_12.md`](_archive/master_tz_2026_06_12.md)) and the older per-repo `woody_v2_tz` umbrella drafts (archived under [`_archive/`](_archive/)).
>
> **Scope & relationship to other docs:** This is the umbrella spec. Component deep-dives stay in each repo's per-component spec (`woody_backend/woody_backend_tz.md`, `woody_admin/woody_admin_tz.md`, `mebellar_app/woody_mobile_tz.md`) and each repo's operational brain (`*/CLAUDE.md`). The detailed, evidence-backed code-health audit behind §7 lives in [`_archive/workspace_audit_2026_06_18.md`](_archive/workspace_audit_2026_06_18.md) (archived — all 25 findings resolved). Architecture & design detail lives in [`architecture/system_design.md`](architecture/system_design.md); the near-term execution backlog in [`planning/`](planning/).
>
> **Provenance:** Sections 1–6 were generated by reading the live source (manifests, routers, migrations, Flutter `lib/`) and reflect the code as it actually is, not as planned. §7 lists only findings that survived independent adversarial verification against the code. **v1.1 (2026-08-01)** documents internal escrow + withdrawals, hybrid guest inbox, push preferences, and analytics privacy enforcement. **v1.2 (2026-08-03)** documents the multi-lingual dynamic seller Oferta (scroll-to-accept, DB stamp, A4 PDF). **v1.3 (2026-08-03)** adds the formal GPD Oferta layout (migration **0093**, version `1.1`), pick-time KYC WebP compress + Documents/Hive draft persistence, and parallel passport upload on accept (mobile `1.0.37+37` Shorebird release). **v1.4 (2026-08-04)** adds lag-safe dual analytics: GA4/BigQuery for sessions + Postgres presence (`last_seen` / device meta) for signed-in users. **v1.5 (2026-09-17)** is a reconciliation pass, not new product scope: it refreshes the mobile dependency table, moves the documented Alembic head from `0089` to the real head **`0102_ai_suggest_quota`**, and records that the Flutter app **no longer has CI** (removed deliberately — see `planning/tech_debt_roadmap.md` T-03). **v1.6 (2026-09-18)** closes the drift v1.5 left open: §2, §3, §5, §6 and the Appendix were re-audited **line by line against the live source** in all four repos. It corrects the backend and admin deploy descriptions (both are Docker Compose, not systemd/`npm ci`), the router count (32 → **42**), the Alembic head in §3 (`0027` → **`0102`**), the AR data model (there *is* a `product_ar_parts` table; per-part models auto-approve and the admin gate sits **before** generation), `/seller/tariff/pay` → **`/seller/tariff/buy`**, and the moderator-scope claim (only **9** of the admin areas are delegable). It adds the 14 tables and 10 routers the spec had never described, and closes three items in §8/§9 that are now answered. Product scope is unchanged.

## Table of Contents

1. [System Overview](#1-system-overview)
2. [High-Level Architecture](#2-high-level-architecture)
3. [Technology Stack](#3-technology-stack)
4. [Core Flows](#4-core-flows) — OTP · Roles · **seller onboarding & legal (B2B Oferta)** · order lifecycle · Payme · **internal escrow / withdrawals** · tariff/wallet · **hybrid inbox + push prefs** · **analytics privacy**
5. [Core Data Models](#5-core-data-models)
6. [API Contracts](#6-api-contracts)
7. [Technical Debt Register](#7-technical-debt-register)
8. [Technical Roadmap (6–12 months)](#8-technical-roadmap-6-12-months)
9. [Open Questions & Uncertainties](#9-open-questions--uncertainties)
10. [Appendix — Document Map & Cross-Repo Contracts](#appendix--document-map--cross-repo-contracts)

---
## 1. System Overview

**Woody** (historically branded *Mebellar* in the mobile app) is a two-sided (B2B2C) **furniture marketplace** built for the Uzbekistan market. It connects buyers (customers) with local furniture manufacturers and retailers (sellers/shops), with platform operators (admins) moderating the marketplace in between.

- **Demand side — Customer.** Browses the catalog, builds a cart, places orders (**cash-on-delivery** or **online via Payme/Click** — deferred until the seller sets the delivery fee), chats with the seller per order, and leaves product reviews. Guests can browse, keep a local cart, and favorite freely; a login gate (phone + OTP) applies only at checkout.
- **Supply side — Seller.** Completes onboarding (including **scroll-to-accept of a dynamic multi-lingual Ommaviy Oferta**), opens a shop, passes verification, lists products (with optional AI fill-from-photos and a photo-to-3D AR pipeline), fulfills orders, buys a subscription tariff (Payme self-serve or manual receipt), tops up the wallet / AR tokens online, and pays commission through a wallet ledger.
- **Operations — Admin.** Super-admins and scoped managers moderate sellers, products, orders, reviews, categories, tariffs, wallet top-ups, **seller withdrawal payouts**, and broadcast notifications. They can toggle Payme/Click visibility via `app_settings.payment_methods`.

**Monetization** comes from two sources, both live in production:
- **Commission + internal escrow** — a tariff-dependent percentage (free 6% / basic 4% / pro 2% / enterprise 1%; trial bonus 2%). Settlement happens on `delivered` via `settle_wallet_on_delivery` (**not** Payme Safe Trade / Split API — Woody never uses Payme's marketplace escrow products). **COD:** the buyer pays the seller in cash; Woody debits `commission` from the seller's wallet. **Online (Payme/Click):** Woody held the buyer's payment; on delivery it credits net income (`order_income` = gross − commission) to the seller's internal wallet; Woody keeps the commission. Sellers withdraw to a card via `wallet_withdrawals` (manual admin payout). The descending commission ladder is the upsell: a higher tier buys a lower rate.
- **Tariff subscriptions** — monthly plans (basic 99 000 / pro 299 000 / enterprise 999 000 so'm) paid via **Payme/Click deep-link** (self-serve, auto-activates on webhook) **or** receipt upload + admin moderation. A 30-day **trial bonus** ("Bonus", 2% commission, 30 products, 3 AI-3D generations) is granted once on a seller's first approval as an acquisition lever.

> Rates and caps live in `woody_backend/app/services/tariffs_seed.py`, which **overwrites** `subscription_plans` on every `woody seed-tariffs` run. Admin-panel edits to a plan are lost at the next seed unless mirrored back into that file.

**Online payments (Payme) are live** — Merchant API webhook (`POST /api/v1/webhooks/payme`, all 6 RPC methods + fiscalization) settles customer orders, seller tariffs, AR-token packs, and wallet deposits. Click is offered in the UI and mints `my.click.uz` checkout URLs, but **has no settlement webhook yet** (payments opened via Click do not auto-confirm). See §4 *Payme payments*.

> **Origin note:** The platform was originally built on Supabase (Postgres + Auth + Storage + Realtime, with `supabase_flutter`, OneSignal, Sentry, and a Telegram bot). On 2026-05-31 Supabase was fully removed from all repos. The current stack is fully independent: own FastAPI backend, own auth (phone + OTP), Cloudflare R2 storage, an in-house WebSocket broker, and Firebase (FCM + Crashlytics + Analytics).

---

## 2. High-Level Architecture

The platform is a **single monorepo containing four subprojects**. All client surfaces (mobile, admin, marketing) talk to **one backend** — `woody_backend` at `api.woody.uz`. Clients hold no database credentials and use no direct third-party data SDKs; every external integration (SMS, object storage, push, AI) is abstracted behind the backend.

### The four modules

| Module | Role | Domain | Talks to |
|---|---|---|---|
| **mebellar_app** | Flutter mobile app — Customer + Seller in one binary, two modes | mobile (Play / App Store) | `api.woody.uz` (REST + WebSocket) |
| **woody_backend** | The single backend — auth, data, storage, realtime, integrations | `api.woody.uz` | Postgres, R2, Eskiz, FCM, Azure OpenAI, Meshy, Payme, Redis |
| **woody_admin** | Internal moderation / management panel | `admin.woody.uz` | `api.woody.uz` (HTTP only; no DB access) |
| **woody_frontend** | Trilingual marketing landing | `woody.uz` | nothing — pure static marketing, no API calls |

### Topology

```
   Flutter (mobile)   ──►  ┌──────────────────────────────┐  ──► Postgres (asyncpg)
   Customer + Seller       │        woody_backend          │  ──► Cloudflare R2 (presigned)
                           │     FastAPI · api.woody.uz     │  ──► Eskiz (OTP SMS)
   Next.js admin      ──►  │   REST (/api/v1) + WebSocket   │  ──► Firebase FCM (push)
   admin.woody.uz          │                                │  ──► Azure OpenAI (AI suggest)
                           │                                │  ──► Meshy (photo → 3D / AR)
   Next.js landing    ──►  │                                │  ──► Redis (optional: WS broker)
   woody.uz (public)       │                                │  ──► Payme Merchant API (webhook)
                           └──────────────────────────────┘
```

How the modules relate:

- **mebellar_app ↔ woody_backend** — the app speaks only REST (routes under `/api/v1`) plus the Woody WebSocket feed (`wss://api.woody.uz/api/v1/realtime/ws`). The same binary runs in customer or seller mode, switched at runtime via a GetIt scope swap and `Phoenix.rebirth`. Uploads (product images, chat attachments, KYC docs, tariff receipts) go through presigned R2 PUT URLs requested from the backend.
- **woody_admin ↔ woody_backend** — the admin is a pure HTTP client of the backend (it owns no database). All reads/writes flow through a single server-only `apiFetch`; RBAC is enforced server-side by the backend on every `/admin/*` route, with the admin's own guards used only for fast redirects and defence in depth.
- **woody_frontend** is decoupled — it never calls the backend. It uses real app screenshots (generated from the mobile app's integration test) for its phone showcase, but has no runtime dependency on the API.
- **Cross-repo contracts** — the status enums (`OrderStatus`, `VerificationStatus`, `ProductStatus`) and the RBAC role rules + moderator scopes are hand-mirrored across backend, mobile, and admin; the backend (`app/domain/enums.py`, `app/security/rbac.py`) is the source of truth.

**Backend internals** are layered: async FastAPI routers (**42** sub-routers under `/api/v1`) → domain services + state machines + background sweepers → asyncpg repositories (each behind a Protocol) → a process-singleton connection pool → Postgres (Alembic-managed). Runtime DB access is **raw asyncpg only**; SQLAlchemy exists solely for Alembic. Auth is own JWT (HS256, access + refresh) minted after Eskiz OTP verification, with the effective role recomputed per request. Realtime is an in-house WebSocket broker that runs in-process for single-worker dev or over Redis pub/sub for multi-worker production. Background sweepers handle the notification dispatcher, tariff expiry, and wallet debt grace/suspension.

### Hosting & domains

| Surface | Domain | Local port (behind nginx) | Deploy |
|---|---|---|---|
| Backend API | `api.woody.uz` | 4001 | CI on push to `main` → SSH → `docker compose build backend` → `docker compose run --rm --no-deps backend woody migrate` → `docker compose up -d --no-build backend` → health-check |
| Admin panel | `admin.woody.uz` | 3002 | CI on push to `main` → SSH → `docker compose up -d --build --no-deps admin_panel` → health-check |
| Marketing landing | `woody.uz` | 3003 | CI on push to `main` → SSH → `docker compose up -d --build frontend` |
| Mobile app | n/a (Play / App Store) | — | `shorebird release android\|ios` (patchable) via `tools/shorebird.sh`; plain `flutter build appbundle` only for non-patchable builds |

> **Deploy-doc drift: RESOLVED (v1.6).** All three server surfaces are **Docker Compose**, not systemd. Verified against the live workflows: `woody_backend/.github/workflows/deploy.yml`, `woody_admin/.github/workflows/deploy.yml`, `woody_frontend/.github/workflows/deploy.yml`.

**Three deploy facts the workflows encode and the runbook must not lose:**

- **The backend migrates between build and swap.** The order is build → `woody migrate` in a throwaway container → *then* replace the running container. Until deploy 256 the swap came first, so a failed migration left new code on the old schema — an outage instead of a clean abort.
- **`--no-deps` is load-bearing on the admin.** `admin_panel` declares `depends_on: backend`; without the flag a cold cache rebuilds the backend too (~8 min on top), overrunning the job's 15-minute timeout.
- **Postgres is Azure Database for PostgreSQL**, not a container. That is a schema constraint, not trivia: Azure refuses `CREATE EXTENSION` unless the extension is listed in the `azure.extensions` server parameter — this is exactly how `pg_trgm` took deploy 256 down. A migration that wants an extension must probe inside a `begin_nested()` SAVEPOINT (a plain `try/except` is not enough: a failed statement poisons the whole transaction). Reference implementation: `0100_catalog_perf.py::_ensure_pg_trgm`.

**The compose stack lives on the server, not in any repo.** Each repo ships its own `Dockerfile` (and `woody_frontend` a reference `deploy/docker-compose.frontend.yml`), but the umbrella `docker-compose.yml` that actually wires `backend` + `admin_panel` + `frontend` together is only on the host. Nothing in git describes the running topology — treat the server as the source of truth for it.

**The edge is Nginx Proxy Manager**, which splits its configuration in two places (http-level zones vs. the proxy host's Advanced tab), so a rate-limit or cache rule needs both halves. The public catalogue reads carry `Cache-Control` so the edge absorbs them; per-IP ceilings for OTP and the AI-suggest endpoint live there too. Reference snippets + the split explained: `woody_backend/deploy/README-nginx.md`, `npm-http-top.conf`, `npm-advanced-api-woody.conf`, `nginx-otp-ratelimit.conf`.

**`mebellar_app/doc/deploy_all.sh`** is a convenience wrapper that pushes all three server repos to `main` (triggering both production deploys) *and* ships a live Android Shorebird OTA patch in one run. It is outward-facing and hard to reverse; its own header says to review `git status` first, since it `git add -A`s each repo.

---

## 3. Technology Stack

All versions below are verified against each repo's real manifest (`pyproject.toml`, `pubspec.yaml`, `package.json`). The mobile column was refreshed 2026-09-17 (v1.5); the backend, admin and frontend columns 2026-09-18 (v1.6).

### Per-module stack

| Aspect | mebellar_app (mobile) | woody_backend (API) | woody_admin (admin panel) | woody_frontend (marketing) |
|---|---|---|---|---|
| **Language** | Dart (SDK `^3.11.5`) | Python `>=3.11` | TypeScript `^5` (strict) | TypeScript `5.6.3` |
| **Framework** | Flutter | FastAPI `>=0.115` (ASGI via uvicorn `>=0.32`) | Next.js `16.2.6` (App Router, Server Components) | Next.js `14.2` (App Router, Server Components) |
| **UI runtime** | Flutter widgets (Material) | — | React `19.2.4` / react-dom `19.2.4` | React `18.3.1` / react-dom `18.3.1` |
| **State / DI** | flutter_bloc `^9.0.0` (Bloc + Cubit), bloc_concurrency `^0.3.0`, get_it `^9.2.1` (DI), equatable `^2.0.8` | FastAPI dependency injection | Server Components (no client state framework) | Server Components (no client state framework) |
| **Routing** | go_router `^14.6.0` (customer: GoRouter; seller: StatefulShellRoute) | FastAPI routers (`/api/v1/*`) | Next.js App Router; edge gate `proxy.ts` | Next.js App Router; `middleware.ts` locale redirect |
| **HTTP / networking** | dio `^5.7.0`, http `^1.2.2` | httpx `>=0.27` (Eskiz, Azure OpenAI, Meshy clients) | server-only `apiFetch` (native fetch) | none (no API calls) |
| **Realtime** | web_socket_channel `^3.0.1` (WSS) | in-house WebSocket broker; redis `>=5.0` (optional pub/sub) | — | — |
| **Datastore** | Hive `^2.2.3` + hive_flutter `^1.1.0` (local cache/cart/favorites); flutter_secure_storage `^10.3.1` (tokens — **held at 10.x on purpose**: a jump to 11 would wipe v9-era entries and sign every user out) | PostgreSQL via asyncpg `>=0.30`; **pgvector `>=0.3.0` (runtime, not dev-only** — `products.embedding vector(1536)` + `image_embedding vector(1024)` back dedupe and visual search); SQLAlchemy `>=2.0` + psycopg `>=3.2` (Alembic only); Alembic `>=1.13` migrations | none — all data via backend | none — static content only |
| **Auth** | phone + OTP against backend; flutter_secure_storage for token pair; no Firebase Auth | own JWT (HS256) via pyjwt[crypto] `>=2.9`; Eskiz OTP | httpOnly cookies → backend JWT; `requireAdmin`/`requireRole` | none (public site) |
| **Object storage** | R2 presigned PUT/GET via `R2UploadClient` | Cloudflare R2 (S3-compatible) via boto3 `>=1.35` | R2 signed URLs via backend round-trip | — |
| **Push / crash / analytics** | firebase_core `^4.13.0`, firebase_messaging `^16.5.0`, firebase_crashlytics `^5.2.7`, firebase_analytics `^12.4.6`, flutter_local_notifications `^18.0.1` (iOS pod suite pinned **Firebase 12.17.0**) | firebase-admin `>=6.5` (FCM; NoOp client in dev/CI) | sonner `^2.0.7` (toasts) | — |
| **Styling / theming** | token bags (PremiumTokens/AuthTokens), bundled fonts (Inter, Manrope, Playfair Display, Plus Jakarta Sans), flutter_svg `^2.0.10`, shimmer `^3.0.0`, iconsax_flutter `^1.0.0`, lottie `^3.1.2` | — | Tailwind CSS v4 + shadcn/ui (`base-nova`), @base-ui/react `^1.5.0`, lucide-react, class-variance-authority, tailwind-merge | Tailwind CSS `3.4.15`, framer-motion `11.11.17`, lucide-react `0.456.0` |
| **Validation** | — | pydantic `>=2.9` + pydantic-settings `>=2.6` | zod `^4.4.3` | — |
| **Notable libs** | yandex_mapkit `^4.2.1`, geolocator `^14.0.2`, image_picker/camera/flutter_image_compress, model_viewer_plus `^1.9.3` + webview_flutter + gal (buyer AR viewer), fl_chart `^0.69.0`, cached_network_image `^3.4.1` (held at 3.x — v4 pulls `material_ui`, which needs a newer `meta` than Flutter 3.44.9 pins), connectivity_plus `^7.3.1` + internet_connection_checker_plus `^3.1.2` (sets the Android floor: AGP ≥ 8.12.1), smart_auth (OTP autofill), talker_flutter (logging), in_app_update / in_app_review | AI suggest: Azure OpenAI / Foundry (`gpt-5-mini`, raw httpx, `api-key:` header); AR pipeline: Meshy `multi-image-to-3d` (raw httpx); Eskiz SMS | @google/model-viewer `^4.3.1` (AR QC), **recharts `^3.9.0`** (dashboard/analytics charts), react-image-crop, next-themes, tw-animate-css | @react-three/fiber `8.17.10` + @react-three/drei `9.114.3` + three `0.169.0` (AI orb only) |
| **Build / test / lint** | flutter test, dart analyze; bloc_test `^10`, mocktail `^1.0.4`, integration_test; flutter_lints `^6` | pytest `>=8.3` + pytest-asyncio `>=0.24`; ruff `>=0.7`; **mypy `>=1.13` (advisory, non-blocking)**; **pglast `>=6.0`** (static SQL-grammar parse in `test_migrations.py`); hatchling build; `woody` CLI | `next build`, eslint `^9` + eslint-config-next, `tsc --noEmit` | `next build` (standalone), next lint, autoprefixer/postcss |
| **Deploy artifact** | AAB / APK (`com.mebellar.app`) | uvicorn behind nginx (port 4001) | `next start` behind nginx (port 3002) | Docker standalone behind nginx (port 3003) |

### Backend external integrations (all behind feature flags / config)

| Integration | Purpose | Gating |
|---|---|---|
| **PostgreSQL** (asyncpg) | Primary datastore on **Azure Database for PostgreSQL**; Alembic-managed schema — baseline `0001`, head **`0102_ai_suggest_quota`** (2026-09-18). The `0027` in earlier revisions of this table was stale by 75 migrations. `pgvector` is a runtime extension here, and Azure gates `CREATE EXTENSION` behind the `azure.extensions` server parameter | `DATABASE_URL` |
| **Cloudflare R2** (boto3) | S3-compatible object storage; presigned PUT (300s) / GET (600s); buckets for documents, product images, shop assets, chat attachments, receipts, AR models, avatars, banners, category images | `R2_*` |
| **Eskiz** (httpx) | OTP SMS delivery; per-locale pre-approved templates (uz/ru/en), 5-digit code bound to template | `ESKIZ_*` |
| **Firebase FCM** (firebase-admin) | Push notifications; real client at boot when service-account set, NoOp default in dev/CI | `FCM_SERVICE_ACCOUNT_PATH`, `NOTIFICATIONS_ENABLED` |
| **Azure OpenAI / Foundry** (raw httpx) | AI fill-from-photos product authoring (`gpt-5-mini`, vision); always 200, never auto-saves | `AI_SUGGEST_ENABLED`, `AZURE_OPENAI_*` |
| **Meshy** (raw httpx) | Photo-to-3D AR pipeline (3 photos → `.glb`); background poll worker | `AR_PIPELINE_ENABLED`, `MESHY_API_KEY` |
| **Azure OpenAI embeddings** (raw httpx) | Text embeddings (`text-embedding-3-small`, **1536**-dim → `products.embedding`, migration 0044) for duplicate detection, and image embeddings (**1024**-dim → `products.image_embedding`, migration 0050) for visual search. Distinct from the vision *suggest* deployment above and separately configured | `AZURE_OPENAI_EMBEDDING_DEPLOYMENT`, `EMBEDDING_DIMENSIONS`, `IMAGE_EMBEDDING_DIMENSIONS` |
| **GA4 proxy → BigQuery** (Cloud Run) | A small standalone service in `woody_backend/ga4_proxy/` (its own `Dockerfile` + `requirements.txt` + `deploy.sh`, deployed to Cloud Run — **not** part of the FastAPI app) that reads GA4 session/funnel data for the admin `/app-usage` surface. The optional BigQuery link is configured in the Firebase console | deployed separately; see `ga4_proxy/README.md` |
| **Redis** (optional) | WebSocket pub/sub fan-out for multi-worker deployments; in-process broker when absent | `REDIS_URL` |

> Each integration is independently optional at boot: the backend starts cleanly (only `/health` green) even with an empty `DATABASE_URL`, no Redis, and notifications/AI/AR disabled.

## 4. Core Flows

> Documented as implemented in `woody_backend/app` and `mebellar_app/lib` as of 2026-06-18. Money is integer so'm throughout. Times are `TIMESTAMPTZ`.

### 1. Authentication — Phone + multi-language SMS OTP (Eskiz) with Android dual-hash autofill

Passwordless: a phone number plus a 5-digit one-time code is the only credential. The backend mints its own HS256 JWTs (no Firebase Auth, no Supabase). Endpoints live in `app/api/v1/auth.py`; orchestration in `app/services/otp.py`; SMS gateway in `app/clients/eskiz.py`; client flow in `mebellar_app/lib/auth/auth_sheet_controller.dart`.

**Request → send (localized template)**
1. Client `POST /auth/otp/request {phone}` with an `Accept-Language` header (`uz` / `ru` / `en`). The router resolves the language via `Depends(get_lang)` and passes it to `OtpService.request_otp`.
2. The phone is normalized to E.164 (`to_e164`); a non-parseable number → `InvalidPhone` → **422 `invalid_phone`**.
3. **Blocked-account gate (before any SMS):** if a live profile exists with `is_blocked`, raise `AccountBlocked` → **403 `account_blocked`** — a banned account never costs a paid SMS.
4. A 5-digit code is generated (`OTP_LENGTH=5`, bound to the approved Eskiz templates), hashed with HMAC-SHA256 + per-row salt (`hash_code`/`make_salt`), and stored in `otp_codes` with `expires_at = now + OTP_TTL_SECONDS` (5 min).
5. **Atomic cooldown claim (SEC-02):** `OtpRepository.claim_request` upserts/resets the code only when the existing row is older than `OTP_REQUEST_COOLDOWN_SECONDS` (60s). If not claimed, raise `RateLimited(retry_after)` → **429 `rate_limited`** with a `Retry-After` header. This closes the read-then-write race that previously let concurrent requests each dispatch a paid SMS.
6. The localized message is selected by `Settings.eskiz_template_for(lang)` (uz is the fallback for any unsupported locale), the literal `{otp}` placeholder is substituted, and `EskizClient.send_sms` dispatches it. Eskiz requires a digits-only `mobile_phone` (no `+`/spaces); the bearer token is fetched lazily, cached in memory, and refreshed once on a 401 then retried.
7. The endpoint returns `{sent: true, cooldown_seconds}`; the client starts a resend countdown from that value.

**Retriever autofill (dual-hash logic, Android) — `_listenForSmsCode`**
- The Eskiz templates each end with the literal 11-char string **`mH1HhnJpGqi`** — this is the SMS Retriever app-signature hash for the **Play App Signing key** of `com.mebellar.app`. Both backend and app hard-code it (`Settings.ESKIZ_MESSAGE_TEMPLATE_*` ↔ `AuthSheetController._playAppSignatureHash`); they must stay byte-identical or autofill silently breaks.
- When the OTP is requested the client starts listening (`smart_auth`), choosing the API by **this running build's** app-signature hash:
  - **Play-installed builds** — signature equals `mH1HhnJpGqi` (matches the template suffix), so `getSmsWithRetrieverApi()` delivers the code **zero-tap**, no dialog.
  - **Debug / sideloaded-release builds** — a different signing key (debug = `yaRHQKhlEo9`), so the silent Retriever can never fire; they fall back to the hash-free `getSmsWithUserConsentApi()`, which shows the system "read this message?" dialog before filling.
- **Two-hash rationale:** the silent Retriever path only fires when the SMS suffix hash matches the *installed* build's signing-key hash. Production (Play) builds carry the Play signing key, so they get the seamless path; locally signed builds would silently never autofill, so they degrade to the User Consent dialog instead of appearing broken.
- Either path writes into `otpCtrl`, which trips the pin field's listener and auto-submits.
- **iOS:** no Retriever — QuickType fills the field via `AutofillHints.oneTimeCode` inside an `AutofillGroup` (`otp_step.dart`).
- **Russian edge case:** the RU template is Cyrillic and exceeds 140 bytes (2 SMS segments), so Retriever can't fire for it — Russian users get the User Consent dialog even on Play builds.

**Verify → JWT issue — `verify_otp`**
8. Client `POST /auth/otp/verify {phone, code}`.
9. **Atomic attempt counting (SEC-01):** `OtpRepository.bump_and_get` increments `attempts` and reads the row in one statement *before* comparison, so concurrent guesses can't all see a stale low count and brute-force the 5-digit code. `attempts > OTP_MAX_ATTEMPTS` (5) → delete row → `OtpAttemptsExhausted` → **429**; expired → delete → `OtpExpired` → **410**; wrong code → `InvalidCode` → **401** (and exhaustion if it was the last allowed attempt).
10. On success the row is consumed (deleted). `find_by_phone` returns **live rows only** — a soft-deleted profile keeps its phone (a partial index frees the number), so a re-login mints a **fresh empty profile** rather than resurrecting the dead one; the old row keeps its order history.
11. Blocked re-check at verify too (the block may have landed between request and verify). Profile is created if absent, or `mark_verified` if previously unverified.
12. `_issue_token_pair` mints `(access, refresh)`. **Admin TTL:** if `get_effective_role(...)` resolves a role (super_admin via `SUPER_ADMIN_PHONE` match or stored manager), the access token gets `JWT_ADMIN_ACCESS_TTL_SECONDS` (1h) instead of the normal `JWT_ACCESS_TTL_SECONDS` (15 min) — the admin panel can't silently refresh mid-render. The refresh `jti` + `exp` are persisted to `refresh_tokens`. `expires_in` mirrors the actual access TTL.

**Refresh / logout — `refresh`, `logout`**
13. `POST /auth/refresh {refresh_token}` verifies a token of `token_type=refresh` (an access token replayed here is rejected). Identity (role, permissions, phone) is **re-resolved from the DB**, never trusted from the old claims, so demotions/blocks/soft-deletes take effect at the next rotation.
14. **Stolen-token defenses:** presenting an already-revoked `jti` triggers `revoke_all_for_user` (scorch the whole chain). A conditional `claim_revoke` (SEC-04) ensures exactly one of two simultaneous refreshes of the same unrevoked `jti` wins; the loser is treated as theft and the chain is scorched. Soft-deleted/blocked accounts also revoke-all → `InvalidRefreshToken` / `AccountBlocked`.
15. `POST /auth/logout {refresh_token}` revokes a single `jti`; idempotent (no-ops on unknown/already-revoked).

**Token format (PyJWT, HS256):** payload `{sub, phone, role, token_type, jti, iat, exp, iss, aud}`; `iss`/`aud` verified on decode. The JWT carries the raw `app_metadata.role`, but the **effective role is recomputed every request** via `get_effective_role()` — never trusted from the JWT for a privilege decision.

**App Store / Play review bypass number:** when both `REVIEW_TEST_PHONE` and `REVIEW_TEST_OTP` are configured, that single number short-circuits both endpoints: `request_otp` returns the normal cooldown without touching the OTP table or sending an SMS (the reviewer can't receive it), and `verify_otp` accepts the fixed `REVIEW_TEST_OTP` via constant-time `hmac.compare_digest`, then runs the **same** find-or-create + blocked re-check + token-mint path as a real login. The bypass is **off by default** (both keys must be set) and `Settings` refuses to boot if `REVIEW_TEST_PHONE == SUPER_ADMIN_PHONE` (which would hand a reviewer super-admin).

> Eskiz templates are sacred: any change to `ESKIZ_MESSAGE_TEMPLATE_{UZ,RU,EN}` (including the trailing `mH1HhnJpGqi` hash) requires re-approval on the Eskiz dashboard, and `OTP_LENGTH` is bound to the approved string.

---

### 2. Roles & Permissions — customer / seller / admin

There are **two RBAC roles** in the backend (`app/security/rbac.py`): `super_admin` and `manager`. "Customer" and "seller" are not RBAC roles — they are the *same* authenticated end-user identity, distinguished by what data they own. Every authenticated request resolves to an `AuthUser` (`app/deps.py`); the three guards are `require_user`, `require_admin`, and `require_role(...)`.

**Customer**
- Any authenticated `AuthUser` with a profile (guarded by `require_user`). No special role.
- Can: browse the public catalog (guest, no auth), manage cart/favorites (`/cart/*`, `/favorites`), get a checkout `/orders/quote`, place orders (`POST /orders`), list/detail their own orders, **self-cancel only a `pending` order** (`POST /orders/{id}/cancel`), review a delivered item once (`POST /reviews`), open/post in per-order chats, register FCM device tokens.
- Gating: every customer query is scoped to `user_id` from the JWT `sub`; there is no cross-user access. Order detail/cancel/review re-verify ownership server-side.

**Seller**
- Also an authenticated `AuthUser` — *becomes* a seller by owning a `sellers` + `shops` row. Endpoints under `/seller/*` use `require_user`, then `_require_shop(user, repo)` → **404 `shop_not_found`** when the caller has no shop. The seller's `seller_id` is the user UUID, never a request parameter (no IDOR).
- Can: onboard (including scroll-to-accept Oferta + optional A4 PDF share), submit KYC verification docs, manage their shop + products (create/update/archive/restore/delete, scoped to their `shop_id`), set order status forward + cancel + set the delivery fee, run AI product fill, manage wallet top-ups + **card withdrawals** + view ledger, subscribe to tariffs + upload receipts, reply to reviews, run the AR photo-to-3D scan.
- Gating: product/order writes are shop-scoped in SQL (`EXISTS … pr.shop_id = $shop` plus a defense-in-depth `NOT EXISTS other shop`); a debt-suspended seller is blocked from product creation (**403 `wallet_suspended`). The Flutter shell additionally demotes to customer mode at boot if the cached seller-approval flag is false.

**Admin (super_admin / manager)**
- Resolved per request via `get_effective_role()`:
  - **super_admin** — granted **only** to the phone in `SUPER_ADMIN_PHONE` (digit-compared via `normalize_phone`). A stored `role='super_admin'` with a mismatched phone is stripped to nothing.
  - **manager** (moderator) — `profiles.app_metadata_role = 'manager'`. Anything else → `None` → guard returns **403**.
- **Moderator scopes (granular):** `profiles.app_metadata_permissions` (jsonb) — `NULL` = full moderator (every delegable area); `["products", …]` = scoped to listed areas only. Delegable areas (`MODERATOR_SCOPES`): orders, products, sellers, shops, customers, reviews, categories, analytics. `settings` and `managers` are **owner-only** and never delegable.
- **Centralized enforcement:** `require_admin` derives the scope from the request path (`scope_for_admin_path`) and checks `has_permission` — super_admin and full managers pass, scoped moderators only on granted areas. Every admin router gets scope gating for free. `require_role("super_admin")` is a factory used at decorator-application time for owner-only operations (e.g. tariff plan CRUD, manager management).
- The super_admin manages moderators via `/admin/managers` (create is an upsert by phone, so a brand-new moderator can OTP-login to the panel immediately). Permissions ride in the JWT (set at login, carried through refresh) so a change takes effect at the moderator's next sign-in; `/me` always returns the *fresh* DB permissions for nav gating.

> The path→role rules + scope list are duplicated in `app/security/rbac.py` and the admin's `lib/auth/roles.ts` — they must be changed in lockstep.

---

### 2b. Seller Onboarding & Legal (B2B) — dynamic multi-lingual Oferta

Woody treats the seller public offer (**Ommaviy Oferta** / vositachilik shartnomasi) as a **B2B trust gate**: the seller must read and accept the live platform contract before onboarding completes. Copy is **not** baked into the mobile binary as the source of truth.

**Dynamic legal document**
1. Live text lives in Postgres `legal_documents` (migrations **0091** + **0092** + **0093**), keyed by `type` + `lang` (`uz | ru | en`). Type `seller_oferta` is the onboarding contract. Migration **0093** refreshes the GPD-style preamble for all three locales and bumps content **`version` to `1.1`**.
2. Public API: **`GET /api/v1/legal/oferta?lang={locale}`** (unauthenticated). Unknown/missing `lang` normalizes to `uz`; if the requested locale row is absent, the API **falls back to uz**. Response includes `{id, type, lang, content, version, updated_at}`.
3. Flutter (`SellerContractScreen` → `LegalDocumentsRepository`) passes `AppLocaleController`’s `languageCode` as `lang`. Client-side injection fills placeholders: `{{SOTUVCHI_NOMI}}`, `{{KOMISSIYA_FOIZI}}`, `{{SANA}}`, optional `{{SHARTNOMA_RAQAMI}}`. Contract number is client-derived: `W-` + first 6 alphanumerics of the JWT `sub` (`generateOfertaContractNumber`). Offline / 404 uses an embedded fallback template with the same placeholders. Body markdown (`**bold**`) is rendered with `flutter_markdown`.
4. Super-admin can replace copy per locale via `PUT /api/v1/admin/legal/documents/{type}` (body carries `content`, `version`, `lang`) so legal updates do not require a store release.

**GPD layout (UI + PDF)**
5. Screen and PDF share a formal CIS/Uzbek GPD composition: (a) city/date + contract title/number header, (b) markdown body from the API, (c) **two-column requisites** table (“TOMONLARNING REKVIZITLARI …”) — left = hardcoded **Woody / Zettacode MCHJ** bank details + director; right = seller legal name, address, phone from the onboarding draft. **No handwritten “Imzo” signature lines** — acceptance is the electronic button press. Empty seller fields are omitted (no `—` placeholders).

**Scroll-to-accept (onboarding UI)**
6. Onboarding step `OnboardingStep.contract` sits after documents and before done. The Accept CTA (**Tasdiqlayman** / localized) stays disabled until the seller scrolls the A4-style body to within ~50px of the bottom (`ScrollController`). Short documents that fit on one screen auto-enable.
7. Accept submits onboarding with the remote `contract_version` the seller actually viewed. Profile later opens the same screen **read-only** (accepted date + version banner; no re-accept gate yet — re-prompt on version bump is a planned follow-up).

**KYC documents (pick → persist → upload)**
8. Gallery pick on `DocumentUploadStep` **compresses to WebP at pick-time** (`compressAndPersistOnboardingKycImage`, q88, min 1600) and writes a stable file under app Documents: `{Documents}/onboarding_kyc/{documentId}.webp`. Encode must **not** run on the contract-accept spinner (that used to stall ~15–30s for two large photos).
9. Paths are stored in the Hive onboarding draft as `document_files` (`Map<documentId, absolutePath>` on `OnboardingDraft`) alongside text fields. On wizard start, `OnboardingBloc` restores paths that still `File.existsSync()`, prunes stale entries, and re-saves. Removing a card deletes that Documents file. Successful submit / reset calls `clearDraft()`, which wipes Hive **and** the `onboarding_kyc` folder.
10. On accept, `POST /seller/onboarding` runs first; then passport front+back upload to R2 (`verification-docs`) + `POST /seller/verification/documents` run **in parallel** (`Future.wait`) using the already-compressed bytes.

**Server stamp**
11. On successful `POST /seller/onboarding`, the backend stamps **`sellers.contract_accepted_at = now()`** and **`sellers.contract_version`** (client-supplied version preferred → else live `legal_documents` version preferring `uz` → else constant `1.0`). Migration **0090** added both columns. Admin seller detail surfaces the stamp for ops audit.

**PDF generation (A4 share)**
12. Sellers can download/share the accepted (or about-to-accept) contract as an A4 PDF via `pdf` + `printing` (`contract_pdf_generator.dart`). Layout matches the on-screen GPD composition (header + body + requisites). Embedded **Inter** Regular/Bold TTFs render both Latin and Cyrillic. Chrome labels localize by `languageCode`.

**i18n**
13. Contract **body** language comes from the API (`?lang=`). Contract **UI chrome** (screen title, scroll hint, Accept, PDF download) uses the app’s `tr()` bundles (uz/ru/en). PDF **labels** follow the same active locale. Placeholder tokens stay language-agnostic so one injection path serves all locales.

---

### 3. Order lifecycle — cart → quote → place → accept → deliver → settle → review-lock

**Cart (guest local Hive → merge on login)** — `mebellar_app/lib/shared/repositories/hybrid_cart_repository.dart`
1. The cart is hybrid: `HybridCartRepository` routes to a **local Hive cart** (guests) or the **backend cart** (`/cart/items`, signed-in) based on session state. Guests add freely with no auth; the login gate is only at checkout.
2. On the guest→signed-in transition, `_mergeGuestCart()` replays the local rows into the server cart (the backend upsert increments quantities — a union, not a toggle), best-effort per item so one unavailable product can't block the rest, then empties the local cart. On sign-out the active backend swaps back to local; the server cart is left intact for the next login.

**Quote (server-computed invoice)** — `POST /orders/quote` → `CustomerOrdersRepository.quote`
3. The checkout screen previews `{subtotal, delivery_fee, installation_fee (potential), installation_selected}`. Pricing is re-derived server-side via the same `_compute_invoice` path that `create()` uses, so the preview can never lie. Delivery applies to every line whose product `has_delivery` (price × qty); installation is the *potential* total and only becomes an applied charge when the customer toggles `want_installation`. Both fees are always server-derived, never client-supplied.

**Place** — `POST /orders` → `CustomerOrdersRepository.create`
4. Pricing/fees are re-derived again at write time (client values ignored). **One order = one shop:** mixing shops → `MultiShopCartNotSupported` (the app already splits the cart into one checkout per shop); this single-shop invariant is what disables the multi-shop IDOR/commission-loss cluster.
5. A single `orders` row is inserted with `status='pending'`, `total_amount = subtotal + delivery_fee + installation_fee`, `delivery_fee`, `installation_fee`, `installation_requested`. Each `order_items` line freezes a snapshot: `price`, `commission_rate` (from the shop's plan at purchase time), `delivery_fee`, `installation_fee`.
6. The router clears the cart only *after* a successful return, so a failed insert can't half-empty the cart.

**Seller accept + delivery-fee lock** — `PATCH /seller/orders/{id}/status`, `…/delivery-fee` → `set_status`, `set_delivery_fee`
7. **Delivery-fee lock:** the seller may set/change the delivery fee **only while the order is `pending`**. The row is locked `FOR UPDATE` for the whole check-then-write; once the seller accepts (confirmed and beyond) the invoice is locked and a fee edit raises `OrderNotPending` → **409**. The new total is re-based in the same statement: `new_total = old_total - old_delivery_fee + new_fee`. The customer is notified via the `orders_notify_delivery_fee_set` trigger (committed in the same transaction).
8. **State machine** (`app/domain/order_policy.py`, mirrored by Flutter `SellerOrderTransitions`): forward lifecycle `pending → confirmed → preparing → shipped → delivered` (confirmed may jump straight to shipped; online payment lands on `awaiting_payment` until paid, then advances to `confirmed`). A seller may cancel from any non-terminal state; a customer may self-cancel only while `pending`. `delivered` / `cancelled` are terminal. Illegal moves raise `IllegalOrderTransition` → **409**. Transitions are validated inside the open transaction so the `FOR UPDATE` lock releases on rollback.

**Delivery SLA (production deadline)** — `stamp_order_deadline` + `OrderSLASweeper`
8a. **Clock starts at `confirmed` only.** Checkout inserts `deadline_at = NULL`. The SLA timer is stamped in the *same transaction* as the transition to `confirmed` — COD via customer fee approval (`CustomerOrdersRepository.approve_fee_adjustment`), online via Payme perform (`awaiting_payment` → `confirmed` + paid), or a seller `set_status` move into `confirmed`. `stamp_order_deadline` reads each line item's `products.production_time_days`, parses the free-text window with `parse_max_production_days` (max of all integers; default **5** days when unparseable), and sets `deadline_at = now() + that many days`. Idempotent: a row that already has `deadline_at` is left alone.
8b. **`OrderSLASweeper`** (hourly, `ORDER_SLA_ENABLED`): selects in-flight rows (`confirmed|preparing|shipped`) with `is_delayed = false` and `deadline_at < now()`, flips `is_delayed = true`, and inserts a `seller_order_delayed` notification for the shop's seller (FCM rides the existing dispatcher).
8c. **Admin "Kechikkan" view** (`GET /admin/orders?delayed=true`): returns only *active* delayed orders — `is_delayed = true AND status NOT IN ('delivered', 'cancelled')`. The list KPI `stats.delayed` uses the same exclusion so resolved history does not inflate the red-zone count. The `is_delayed` flag itself is retained on terminal rows for audit.

**SLA Penalty & Enforcement** — soft demotion + hard suspend (migration 0087)
8d. **Soft penalty (`sellers.sla_penalty_multiplier`, default `1.0`):** scales public catalog ranking. Values below `1.0` demote the shop in `newest` / `popular` / `recommended` (and act as a tiebreaker on price sorts). Catalog SQL multiplies popularity and recommended scores by `COALESCE(se.sla_penalty_multiplier, 1.0)` and leads relevance `ORDER BY` with that expression `DESC`.
8e. **Hard penalty (`sellers.is_suspended_due_to_sla`, default `false`):** moderator-toggled via `PATCH /admin/sellers/{id}/sla-suspend` (`{suspended, sla_penalty_multiplier?}`). While set, `SellerWalletRepository.activity_block_reason` returns `seller_suspended_sla` and seller mutations (create/update product, accept order, set status / delivery fee) return **403**. Catalog stock CASE and checkout pricing treat SLA suspend like debt suspend (effective stock 0 / unpurchasable). Cash SLA fines use the existing admin wallet `adjustment` path — no separate fine endpoint.
8f. **Admin UI:** seller detail shows an SLA badge + suspend toggle + multiplier control; copy points moderators to `/wallets` for monetary adjustments.

### 4b. Payme payments (live) + Click (link-only)

**Deferred customer checkout** — place-order stores the method; charge happens later
1. Customer picks `cash | payme | click` at checkout (`CheckoutCubit`). `POST /orders` stores `payment_provider` / `payment_status=unpaid` and leaves the order `pending` — **no** Payme deep-link is minted at place-order time.
2. Seller accepts and proposes the exact delivery fee; customer approves (`approve_fee_adjustment`):
   - **cash** → `confirmed` (+ SLA `deadline_at` stamped).
   - **payme / click** → `awaiting_payment` (SLA clock does **not** start yet).
3. Customer taps **Pay** on order detail (`_PayNowBar`) → `POST /orders/{id}/pay/{payme|click}` → `PaymentLinkService` returns a hosted checkout URL → app marks a `PendingPayment` and opens it via `url_launcher` (`LaunchMode.externalApplication`).
4. On return / cold start, `PaymentRecoveryGate` polls settlement; realtime / refetch drops `awaiting_payment` once paid so the Pay bar disappears.

**Payme Merchant API** — `POST /api/v1/webhooks/payme` (`app/api/v1/payme_webhook.py` → `PaymeMerchantService`)
5. Public JSON-RPC endpoint (HTTP Basic = `PAYME_MERCHANT_KEY`; login `Paycom` or `PAYME_MERCHANT_ID`). Always HTTP 200 with `{result}` or `{error}`.
6. All **six** classic methods are implemented: `CheckPerformTransaction`, `CreateTransaction`, `PerformTransaction`, `CancelTransaction`, `CheckTransaction`, `GetStatement` (`app/services/payme_merchant.py` + `payme_repos.py`).
7. **Strict order gate (unified with link minting):** CheckPerform / Create accept an order **only** when `status == 'awaiting_payment'` AND `payment_status == 'unpaid'`. A still-`pending` order (fee not set) returns Payme `-31050` (`invalid_account` / not payable) — same rule as `POST /orders/{id}/pay/payme`.
8. Account fields (cabinet-configurable): `order_id`, `ar_purchase_id`, `subscription_id` (= `subscription_receipts.id`), `deposit_id`. Amounts are **tiyin**.
9. **Fiscalization:** `CheckPerformTransaction` returns `detail.receipt_type` + line items (product MXIK / package codes, delivery + installation lines, VAT from `PAYME_VAT_PERCENT`) for Soliq. Dedicated MXIK env keys cover AR / subscription / wallet service lines.
10. **`PerformTransaction` settlements** (atomic with the tx state flip):
   | Target | Intent table | Side-effect |
   |---|---|---|
   | Order | `orders` | `payment_status=paid`, provider=`payme`; if status was `awaiting_payment` → `confirmed` + `stamp_order_deadline` |
   | AR tokens | `ar_token_purchases` | credit tokens + `paid` |
   | Tariff | `subscription_receipts` | `activate_subscription_receipt` |
   | Wallet | `wallet_deposits` | `apply_wallet_delta` + deposit `paid` |
11. **`CancelTransaction` after perform (refund):** state 2 → −2. **Strict order gate still applies historically** — only orders that were payable under `awaiting_payment` should have been performed. For an **order** target (unless already `delivered` → Payme `-31007`), the same DB transaction sets `payment_status='refunded'`, **`status='cancelled'`**, `cancel_reason_code='payment_refunded'`, clears `is_delayed`, and writes a human `cancellation_reason` — **unwinding the order lifecycle** like a normal cancel (inbox/SLA treat it as cancelled, not paid). Subscription / wallet / AR targets revert their credits as before.
12. Checkout deep-link shape: `{PAYME_CHECKOUT_BASE}/{base64(m=…;ac.*=…;a=<tiyin>)}` (prod default `https://checkout.paycom.uz`).

**Seller self-serve rails** (same `PaymentLinkService` + recovery gate)
13. `POST /seller/tariff/buy`, `POST /seller/ar-tokens/buy`, `POST /seller/wallet/deposit` — body carries `provider: payme|click`; app opens the URL and polls `…/status` / `PaymentRecoveryGate` on resume. Manual P2P (screenshot → admin approve) remains as a parallel path for tariff and wallet.

**Click** — deep-link only today
14. `PaymentLinkService._click_url` mints `https://my.click.uz/services/pay?…`. Admin can toggle Click via `payment_methods`. **There is no Click Prepare/Complete webhook** — choosing Click creates a pending intent that never auto-settles until a merchant callback is built. Treat Payme as the only live auto-settlement rail.

**Delivered → wallet settlement (COD commission vs online escrow)** — `settle_wallet_on_delivery` (`app/services/wallet.py`)
9. **Delivery is the completion event:** the first time an order transitions to `delivered` (`status == 'delivered' and previous != 'delivered'`), wallet settlement runs **in the same transaction** as the status flip (`SellerOrdersRepository` / equivalent). Woody does **not** use Payme Safe Trade / Split — funds for online orders sit with the platform until delivery, then Woody credits the seller's **internal** wallet.
10. **Branch by `payment_provider`:**
    - **Cash / COD** → `charge_order_commission`: debit `ROUND(SUM(price × quantity × commission_rate / 100))` as ledger type `commission` (seller already collected cash). Idempotent via `wallet_tx_commission_once_idx`.
    - **Online (`payme` / `click`)** → `credit_order_income`: credit **net** `(gross − commission)` as ledger type `order_income`. Commission stays with Woody (no separate commission debit row). Idempotent via `wallet_tx_order_income_once_idx` (migration 0088). Zero/negative net produces no row.
11. A zero-rate plan would produce no COD commission row — no seeded plan is currently zero-rate (trial bonus is 2%).

**Seller payouts (manual card withdrawal)** — migration `0088_wallet_withdrawals`
12. Seller requests a card payout: `POST /seller/wallet/withdraw` `{amount, card_number}` → row in `wallet_withdrawals` (`pending`) and an immediate `withdrawal_hold` debit via `apply_wallet_delta` (funds leave spendable balance while admin reviews).
13. Admin reviews: `GET/PATCH /admin/wallets/withdrawals/{id}` (`super_admin`). **Approve** keeps the hold (money already removed; ops pays the card out-of-band). **Reject** credits `withdrawal_refund` and stamps `rejection_reason`. Seller lists history via `GET /seller/wallet/withdrawals`.

**Review lock**
14. A review is allowed only for a **delivered** order line the caller bought: `ReviewsRepository.create` resolves `order_item → product/shop/order`, verifies `user_id` ownership (→ `OrderNotFound`), requires `status == 'delivered'` (→ `ReviewNotAllowed` / **409**, checked app-side for a clean error ahead of the DB trigger), and a unique constraint enforces one review per order item (→ `ProductAlreadyReviewed`). The "rated" state is backend-derived; the app surfaces the in-app review prompt for 4★+.

---

### 4. Tariff / trial / wallet soft-freeze + grace

**Tariffs & trial**
- Every seller is on the `free` plan by default. Plans (`subscription_plans`) carry `commission_rate`, `max_products`, `max_images_per_product` (`-1` = unlimited), price, and localized features. Admin owns plan CRUD (`require_role("super_admin")`) and receipt moderation.
- A seller subscribes by **Payme/Click deep-link** (`POST /seller/tariff/buy` → auto-activate on Payme `PerformTransaction`) **or** by uploading a payment receipt (`/seller/tariff/...`); admin approves/rejects the manual path. Approval stamps `subscriptions.expires_at`. A 30-day first-approval **trial bonus** ("Bonus" plan) drops commission to **2%** for the window — down from the free plan's 6% — and unlocks sets, 30 products, and 3 AI-3D generations. It is never purchasable (price 0) and renders only as the current-plan card.
- **Tariff expiry sweeper** (`app/services/tariff_expiry.py`, same asyncio-loop shape as the wallet sweeper): every `approved` subscription whose `expires_at` has passed is flipped to `expired`, the shop is repointed at `free` via the audited `admin_set_shop_plan`, products over the free limit are archived, and the seller is notified. Sellers near expiry get a one-shot `tariff_expiring` notification (deduped by expiry date).

**Wallet soft-freeze + 48h grace** — `app/services/wallet.py`
- Money is integer so'm; `credit_limit` is a non-negative magnitude and the balance floor is `-credit_limit` (free tier 0 → floor 0; pro 500 000 → floor −500 000). Automatic ledger types: COD `commission` debits, online `order_income` credits, `withdrawal_hold` / `withdrawal_refund` for payouts.
- Every balance mutation funnels through `apply_wallet_delta()` inside the caller's transaction: it locks the seller row `FOR UPDATE`, appends an immutable `wallet_transactions` ledger row with the running `balance_after`, updates `wallet_balance`, and re-evaluates the debt state machine — nothing else writes the balance.
- **Soft-freeze ladder** (`_recalc_debt_state`):
  1. `balance >= floor` → normal service. Recovery is **instant**: any credit that lifts the balance back to the floor clears `is_suspended_due_to_debt` + `debt_grace_period_until` and fires a reactivation notification.
  2. `balance < floor` and no clock running → start a **48h grace window** (`WALLET_DEBT_GRACE_HOURS`), set `debt_grace_period_until`, send a warning notification with the shortfall + deadline. Sinking below the floor only *starts* the clock; it never freezes immediately.
  3. Grace lapses still below the floor → `is_suspended_due_to_debt = true` (flipped by the sweeper). Suspended products **stay visible** in the catalog but report stock 0 / unpurchasable (the order-create pricing path also raises `SellerSuspended` for stale clients).
- **`WalletDebtSweeper`** (default 5 min, `WALLET_DEBT_ENABLED`) is the time half of the machine and runs recovery-first each pass so a recovered seller is never frozen in the same sweep: `_reactivate_recovered` (e.g. a plan upgrade raised `credit_limit`), `_suspend_lapsed` (grace window passed), `_start_grace_overdrawn` (a tariff *downgrade* lowered the floor under the balance — gives the same grace window, not an instant freeze). Each step re-locks and re-checks under `FOR UPDATE` so a between-scan recovery clears the flags instead of freezing.
- **Top-ups:** seller can (a) self-serve via Payme/Click (`POST /seller/wallet/deposit` → Payme webhook credits) or (b) submit `{amount, payment_screenshot_path}` → `pending`; admin approves (credits via `apply_wallet_delta` type `topup`, notifies) or rejects (with reason, notifies). Admin can also post a signed manual `adjustment` through the same choke point.
- **Withdrawals (escrow payout):** see §4b steps 12–13 — `wallet_withdrawals` + `withdrawal_hold` / `withdrawal_refund`. Account deletion refuses a seller with unwithdrawn positive balance (`409 has_unwithdrawn_funds`).

---

### 5. Chat + hybrid inbox + FCM (prefs, per-chat tag, read receipts)

**Per-order chat** — `app/services/chat_repos.py`, shared Flutter module `lib/shared/chat/`
- One `chats` row per `order_id` (UNIQUE). The **customer lazy-creates** the row on first message; the **seller can never spawn a chat**. Chat stays open forever, even after delivered/cancelled — the status banner reflects the current order status (with a "leave a review" CTA on delivered orders, customer side).
- `post_message` is the single source of truth for the unread bump: it inserts the message and increments the **other** side's counter (`seller_unread_count` when a customer sends, vice versa) plus stamps the last-message snapshot. Chat no longer persists to the `notifications` table (the FCM push is sent directly from the app layer).

**Hybrid notifications inbox (guest-safe)** — `NotificationsCubit`
- **Public / System tab:** guests and signed-in users fetch broadcasts via unauthenticated `GET /catalog/news` (plus admin broadcasts that land in personal inbox when signed in). Guest read-state for news lives in Hive (`newsReads`).
- **Personal / Orders tabs:** require auth — `GET /notifications` (JWT-scoped). Guests see a login CTA on those tabs only; browsing public news never forces sign-in.
- Live updates: Woody WebSocket `notification` events + FCM foreground hook reload the cubit after sign-in.

**Push preference architecture** (Settings → "Bildirishnomalar")
- **`promo_push_enabled`** (`profiles`, default `true`; Hive `promo_push_enabled`): Flutter owns FCM **topic** membership for marketing. On → `FirebaseMessaging.subscribeToTopic('news')`; off → `unsubscribeFromTopic('news')`. Also mirrored on `PATCH /me` for cross-device hydration. Cold permission prompt skips subscribe when the Hive flag is false.
- **`order_push_enabled`** (`profiles`, default `true`; Hive `order_push_enabled`): synced via `PATCH /me`. `NotificationDispatcher.dispatch_one` still claims the inbox row + realtime fan-out, but **skips OS-level FCM** for ORDER-category types when the flag is false (`_user_allows_os_push`). Chat / system / admin pushes are unaffected by this flag.

**Read receipts** — `mark_read`
- `POST /chats/{id}/read` zeroes the reader's own unread counter and stamps `read_at = now()` on all unread messages from the **other** side.
- When something actually changed (and not a self-chat), the sender is sent a `chat_read` realtime event over the WebSocket so their chat-list receipt flips live from delivered (✓) to read (✓✓) instead of lagging until the next refetch.

**FCM new-message push (per-chat tag + dismiss-on-read)** — `_push_chat_message` + `app/clients/fcm.py`
- After persistence + WebSocket delivery have committed, `_push_chat_message` fans a best-effort push to the recipient's `device_tokens` (a push failure never 500s the POST). The payload is built by `build_chat_push`.
- **Per-chat collapse:** the push carries `android_tag = "chat:{chat_id}"` and `thread_id = str(chat_id)`. On Android the tag collapses repeat messages from one chat into a single tray entry; on iOS `apns-collapse-id` + `Aps.thread_id` group/keep-latest. The tag format **must** match the Flutter side (`PushService.chatNotificationTag`) so the app can **cancel a chat's tray notifications once it's read**.
- The real `FirebaseFcmClient` offloads each send to a worker thread (firebase-admin is sync), chunks tokens 500/multicast, and returns `Unregistered`/`SenderIdMismatch` tokens which are pruned from `device_tokens`; transient failures are logged and the token kept. `NoOpFcmClient` is the dev/CI default (selected at boot when `FCM_SERVICE_ACCOUNT_PATH` is unset).
- General order/fee/verification/moderation pushes flow through the separate `NotificationDispatcher` (LISTEN + sweep), which also publishes to the realtime broker; those use no `android_tag`/`thread_id`, and honour `order_push_enabled` + per-row `data.send_push`. Realtime degrades gracefully — the app falls back to refresh-on-open + FCM foreground push if a socket event doesn't arrive.

### 5b. Analytics privacy + dual pipeline (App Store / Play compliance)

Settings **"Foydalanish statistikasi"** (`analytics_collection_enabled` in Hive; default `true`) is a hard collection gate, not UI-only:
- **Toggle / cold boot:** `applyAnalyticsCollectionEnabled` → `FirebaseAnalytics.setAnalyticsCollectionEnabled` **and** `FirebaseCrashlytics.setCrashlyticsCollectionEnabled(enabled && !kDebugMode)`, plus Meta/`FacebookAnalyticsService.setPrivacyCollectionAllowed` (AND'd with iOS ATT). When off, Flutter `PresenceService` also **stops** its timer (historical Postgres rows are retained).
- **Boot leak prevention:** after `Firebase.initializeApp`, Analytics + Crashlytics are muted until Hive opens and the preference is applied in `_bootstrapAndRun` (before `runApp` / `FirebaseAnalyticsObserver`).
- Debug builds never enable Crashlytics collection regardless of the toggle.

**Track A — Firebase / GA4 (sessions, peak hours, funnels):** Admin `/app-usage` already proxies `GET /admin/analytics/engagement`. Optional user property `app_mode` (`customer`|`seller`) is set from `AppModeCubit` when privacy is on. Device model is automatic in GA4.

**BigQuery ops checklist (Firebase Console — not app code):**
1. Firebase Console → project → **Integrations** → **BigQuery**.
2. Link the **GA4** property (Analytics) export; choose daily (and optionally streaming) export.
3. Confirm dataset appears in GCP BigQuery (events + device/session tables) before relying on warehouse queries for peak hours / session duration beyond `/app-usage`.

**Track B — Postgres presence (signed-in only, migration 0094):** Account-level `profiles.last_seen_at` + `last_device_model` / `last_os` / `last_os_version` / `last_app_version`. Per-FCM-device enrichment on `device_tokens` (`device_model`, `os_version`, `app_version`, `last_seen_at`). Client: `POST /me/presence` via lag-safe `PresenceService` (≤1 ping / 5 min, 3s timeout, `unawaited`, privacy + auth gated). FCM register may send the same meta. Admin customer **Faollik** and seller detail surface last seen + device/OS/app version. Guests and session-duration history are **out of scope** (session metrics stay in GA4).

**Native / release note:** adding `device_info_plus` is a **native plugin** → next store build must be `./tools/shorebird.sh release …` (not a Dart-only Shorebird patch). Deploy order: backend `woody migrate` (0094) → admin → app store release.

## 5. Core Data Models

The schema is Postgres (asyncpg at runtime; Alembic for migrations). The baseline (`0001_baseline_schema.sql`) created 29 tables with all Supabase-specific bits stripped (no RLS, no `auth.users` FKs, no realtime publication, no pg_net triggers). `profiles` is the canonical user table; Python creates rows at OTP-verify time. All timestamps are `TIMESTAMPTZ`. Status-like columns are `text` + `CHECK` constraints, never DB enums.

**Head `0102` defines 51 live tables + 1 materialized view** — the 29 baseline tables plus 22 surviving later additions (23 were created; `payment_cards` was dropped again in `0052`). The subsections below describe all of them; the groups marked *(v1.6)* were added to this spec in the 2026-09-18 reconciliation and had never been documented anywhere.

### Identity & users

- **`profiles`** — canonical user row. `id` (uuid PK), `full_name`, `phone`, `preferred_language` (default `uz`), `avatar_url`, `is_seller_pending`, `is_deleted`, `created_at`/`updated_at`. RBAC role + scopes live here as `app_metadata_role` / `app_metadata_permissions` columns (added in later migrations; `phone` is UNIQUE). Push prefs (migration **0089**): `promo_push_enabled` / `order_push_enabled` (both `boolean NOT NULL DEFAULT true`). Presence (migration **0094**): `last_seen_at`, `last_device_model`, `last_os`, `last_os_version`, `last_app_version`. A profile is a customer by default; becoming a seller adds a `sellers` row.
- **`sellers`** — seller extension, `id` PK **references `profiles(id)`** (1:1). `verification_status` (default `pending`), `business_type`, `legal_name`, `contact_phone`/`contact_email`, `telegram_username`, `rejection_reason`, passport paths. **Oferta acceptance (migration 0090):** `contract_accepted_at` (`TIMESTAMPTZ`, nullable until onboarding accept) and `contract_version` (`text`, stamped from the live `legal_documents` row / client). Wallet columns (migration 0027): `wallet_balance` (signed so'm), `credit_limit` (≥0 floor magnitude), `is_suspended_due_to_debt`, `debt_grace_period_until`. `first_approved_at` (migration 0020) gates the one-time trial bonus. **Assisted onboarding (migration 0101):** `created_by_admin_id` (FK → `profiles`, `ON DELETE SET NULL`) + `assisted_created_at`; `created_by_admin_id IS NOT NULL` is the "assisted" predicate. **AR tokens (0039):** `ar_credits` — the balance the per-part 3D pipeline debits. SLA penalties (migration 0087): `sla_penalty_multiplier` (float, default `1.0` — soft catalog demotion) and `is_suspended_due_to_sla` (bool, default `false` — hard activity block, moderator-toggled).
- **`shops`** — one per seller, `seller_id` UNIQUE → `sellers(id)`. `name`, `address`, `description`, `logo_url`/`cover_url`, `brand_color`, `working_hours` (jsonb), `latitude`/`longitude`, `is_active`, `plan_id` → `subscription_plans(id)`, and `visibility` (`public | hidden | unlisted`, the third added in 0024). A storefront query treats `unlisted` as link-only and `hidden` as fully closed.
- **`seller_verifications`** / **`verification_documents`** — verification state machine (`none|pending|in_review|approved|rejected`) and uploaded document storage paths, both keyed on the seller profile id.

### Catalog: categories, products, attributes

- **`categories`** — `id`, `name`/`name_uz` (required) + `name_ru`, `subtitle`, `image_url`/`icon_url`, `sort_order`, `is_active`. Seeded in 0005.
- **`subcategories`** — `category_id` → `categories`, `name`.
- **`products`** — core listing. `category_id` (required), `subcategory_id` (nullable), `shop_id`, `seller_id` (required). `name`, `description`, `price numeric(12,2)`, `stock`, `images text[]`, `colors text[]` (GIN-indexed), and a free-form `attributes jsonb` blob. Typed logistics fields: `width_cm`/`height_cm`/`depth_cm`, `material`, `production_time_days`, `has_delivery` + `delivery_price`, `has_installation` + `installation_price`, `warranty_months`. `status` ∈ `draft|pending_review|approved|rejected|archived` with `rejection_reason`. **AR fields** (0034, extended 0035/0036): `ar_video_url`, `ar_model_url`, `ar_usdz_url`, `ar_model_bytes`/`ar_usdz_bytes`, `ar_source_images jsonb`, `ar_status`, `ar_rejection_reason`, `ar_meshy_task_id` (0035), `ar_error_reason` (0036). **`ar_status` has six values**, not five — `none|processing|pending_review|approved|rejected|failed` (`failed` added in 0036; mirrored by `app/domain/enums.py::ArStatus`). Since migration 0059 these columns are a **denormalised cache** of the primary visible part, not the source of truth — see §5.9. **`products.embedding vector(1536)`** (0044) and **`image_embedding vector(1024)`** (0050) back duplicate detection and visual search (pgvector). `created_by_admin_id` (0101) is the assisted-onboarding audit trail — its non-NULLness *is* the "admin-drafted" predicate; there is no separate flag.
- **`product_variants`** / **`product_images`** — per-variant SKU/color/price and ordered image rows (the live app primarily uses `products.images[]`; these tables back richer variant/image modeling).
- **`attribute_definitions`** — per-category (or per-subcategory, XOR-constrained) attribute schema: `key`, `label_uz`/`label_ru`, `data_type` ∈ `select|multiselect|number|text|bool`, `unit`, `is_required`, `sort_order`. UNIQUE per (category, key).
- **`attribute_options`** — allowed values for a `select`/`multiselect` definition: `value`, `label_uz`/`label_ru`. Seeded in 0011 (81 defs + 168 options across 8 categories); reshaped for beds in 0012. Dimensions are stored both as typed `products.*_cm` columns and (per-piece, for sets) inside `products.attributes` jsonb.

### Cart & favorites

- **`cart_items`** — `(user_id, product_id)` UNIQUE, `quantity` (1–99 CHECK), `product_snapshot jsonb`. (Hybrid local-Hive + server cart; this is the server side, merged on login.)
- **`favorites`** — `(user_id, product_id)` UNIQUE, `product_snapshot jsonb`.

### Orders, items, reviews

- **`orders`** — `user_id` → `profiles`, `total_amount numeric(12,2)` (grand total = subtotal + fees since 0028), `status` (default `pending`; lifecycle includes `awaiting_payment|confirmed|preparing|shipped|delivered` + cancelled), `delivery_address`. Invoice fees (0028): `delivery_fee`, `installation_fee`, `installation_requested`. Post-placement fee negotiation: `proposed_delivery_fee`, `fee_adjustment_note`, `fee_adjustment_status` (`pending_customer|approved|rejected`). Cancellation (0030): legacy `cancellation_reason` plus structured `cancel_reason_code` + `cancel_reason_text`. Payment (0068+): `payment_provider` (`cash|payme|click`), `payment_status` (`unpaid|paid|refunded`), `payme_receipt_id`. Delivery SLA (0086): `deadline_at TIMESTAMPTZ` (nullable until `confirmed`) and `is_delayed BOOLEAN NOT NULL DEFAULT false` (sweeper-set; terminal statuses stay out of the admin delayed filter).
- **`order_items`** — `order_id`, `product_id`, `quantity` (>0), `price numeric(12,2)`, `color_slug`. Frozen-at-purchase snapshots: `commission_rate` (0020), `delivery_fee`/`installation_fee` (0028).
- **`cancellation_reasons`** — catalogue of predefined reasons per `role` (`customer|seller`) with `title_uz/ru/en`, `code`, `sort_order`. Each role carries an `other` code (UNIQUE on (role, code)).
- **`reviews`** — one per `order_item_id` (UNIQUE). `order_id`, `product_id`, `shop_id` (resolved by trigger), `customer_id`, `rating` (1–5 CHECK), `comment`, `seller_reply` + `seller_replied_at`. A DB trigger enforces the order is `delivered` and the reviewer is the buyer before insert.
- **`payme_transactions`** — Merchant API ledger (0053+): Payme `id`, `state` (−2/−1/1/2), amounts in tiyin, `target_kind` (`order|ar_tokens|subscription|wallet_deposit`) + target FK, create/perform/cancel times. Drives idempotent `PerformTransaction` / `CancelTransaction`.

### Chat & notifications

- **`chats`** — one per `order_id` (UNIQUE). `customer_id`, `shop_id`, `last_message_at`, `last_message_preview`, `customer_unread_count`/`seller_unread_count`.
- **`chat_messages`** — `chat_id`, `sender_id`, `sender_role` (`customer|seller`), `body` (≤4000) and/or `attachment_url` (at least one required), `read_at`. `mark_chat_read(chat_id, user_id)` clears counts + stamps `read_at`.
- **`notifications`** — `user_id`, `title`, `body`, `type` (large CHECK enum: order lifecycle, seller events, fee adjustments, `admin_broadcast`, `chat_message`, tariff lifecycle, wallet lifecycle…), coarse generated `notification_type` (`ORDER|SYSTEM|PROMO|CHAT`), `is_read`, `reference_id`, `data jsonb` (carries `mode`/`route` for deep-linking; admin may set `send_push:false` for inbox-only). The dispatcher claims the row, optionally skips FCM when `order_push_enabled` is false for ORDER types, and still fans out realtime.
- **`device_tokens`** — `token` PK, `user_id`, `platform` (`android|ios|web`). Pruned when FCM reports a token unregistered.
- **`news`** — public broadcast feed served at `GET /catalog/news` (no auth); Flutter merges it with personal `/notifications` for the hybrid inbox.

### Tariffs / subscriptions

- **`subscription_plans`** — `code` UNIQUE (`free|basic|pro|enterprise`; plus `trial` from 0020), `name`, `price_monthly`, `max_products`, `max_images_per_product`, `commission_rate`, `credit_limit` (0027), `features_uz`/`features_ru` (jsonb), `is_recommended`. Seeded in 0013.
- **`subscriptions`** — active/pending plan per seller (`plan_code`, `billing_period`, `status`, `started_at`, `expires_at`).
- **`subscription_history`** — audit of plan moves (`from_plan_id`/`to_plan_id`, `reason`); written by `admin_set_shop_plan()`, the single choke point that also copies the plan's `credit_limit` onto the seller.
- **`subscription_receipts`** — payment-screenshot moderation flow: `plan_code`, `billing_period`, `amount`, `payment_screenshot_path`, `status` (`none|pending|approved|rejected|cancelled`), approve/reject/expiry timestamps, `rejection_reason`.
- Product/image quota enforcement is gated by `is_tariff_enabled()` (an `app_settings` flag) via `BEFORE INSERT` triggers on `products`.

### Wallet / ledger (migrations 0027 + 0088)

- **`wallet_transactions`** — append-only signed so'm ledger: `seller_id`, `amount` (signed), `balance_after` (denormalised running balance), `type` (`commission|topup|adjustment|refund|order_income|withdrawal_hold|withdrawal_refund`), `order_id`, `note`, `created_by`. Partial UNIQUE indexes make delivery settlement idempotent: `(seller_id, order_id) WHERE type='commission'` and `(seller_id, order_id) WHERE type='order_income'`.
- **`wallet_topups`** — top-up requests via the receipt pattern: `amount` (>0), `payment_screenshot_path`, `status` (`pending|approved|rejected|cancelled`), `rejection_reason`. Separate from subscription receipts (no plan/period). Soft-freeze ladder: balance below `-credit_limit` opens a 48h grace window, then the sweeper flips `is_suspended_due_to_debt`.
- **`wallet_withdrawals`** (0088) — seller card payout requests: `seller_id`, `amount` (>0), `card_number`, `status` (`pending|approved|rejected`), `rejection_reason`, timestamps. Request holds balance (`withdrawal_hold`); admin approve keeps the hold; reject refunds via `withdrawal_refund`.

### Legal documents (seller Oferta)

- **`legal_documents`** (migrations **0091** / **0092** / **0093**) — platform legal copy served to clients. Columns: `id`, `type` (e.g. `seller_oferta`), `lang` (`uz|ru|en`, UNIQUE with `type`), `content` (markdown-ish text with `{{SOTUVCHI_NOMI}}` / `{{KOMISSIYA_FOIZI}}` / `{{SANA}}` / optional `{{SHARTNOMA_RAQAMI}}` placeholders), `version` (seller_oferta currently **`1.1`** after GPD preamble seed in **0093**), `updated_at`. Public read: `GET /legal/oferta?lang=`; admin upsert per locale: `PUT /admin/legal/documents/{type}`.

### Shop services & app settings

- **`shop_services`** — PK `(shop_id, service_type)`: per-service config (`enabled`, `min_order_amount`, `fee_amount`, `warranty_months`, `installment_months`).
- **`app_settings`** — `key` PK, `value jsonb`. Holds the `tariff_enabled` master switch and the **receiving card** (`receiving_card` key → `{card_number (16 digits), card_holder, bank_name}`), used by sellers to know where to send tariff/top-up payments. Admin manages it under `/admin/settings/receiving-card`; sellers read it via `/seller/tariff/...`.
- **`news`** / **`banners`** — marketing content: news (title/body/data jsonb, `published_at`, `is_active`; public catalog read) and home banners (`image_url`, `action_type`/`action_value`, `sort_order`, `is_active`).

### AR models — per-part, in its own table *(corrected v1.6)*

> Earlier revisions of this spec said AR state lives only on `products` with "no separate table", and described the lifecycle as ending in **admin QC**. Both statements were wrong as of migration **0059** (2026). What follows is the current model.

- **`product_ar_parts`** (migration **0059**, states widened in **0071**) — **the canonical AR table.** One row per independently-scannable component of a product: a garnitur carries one row per piece (`bed`, `wardrobe`, `dresser`, …), a single-piece product carries one row keyed `single`. Columns: `product_id` → `products` (CASCADE), `part_key` (UNIQUE per product), `label`, `sort_order`, `ar_status`, `ar_model_url`/`ar_usdz_url` + `ar_model_bytes`/`ar_usdz_bytes`, `ar_meshy_task_id`, `ar_source_images jsonb`, `ar_error_reason`, `free_scan_used`, `ar_current_gen_billed`, `is_ar_visible`, per-part `height_cm`/`width_cm`/`depth_cm`, timestamps. `0059` backfilled one `single` part per previously-approved legacy model, so nothing shipped broke.
- **`product_ar_parts.ar_status` has six values** — `none | pending | processing | approved | rejected | failed`. `pending` and `rejected` were added by **0071** when the workflow pivoted, and they are the important ones.
- **There is no post-generation admin QC.** The admin gate moved to **before** generation. The seller no longer triggers Meshy: they *request* a model (part → `pending`, 1 AR token locked); an admin reviews the queue at `/admin/ar-requests`, picks the best product photos and sends them to Meshy (→ `processing`), or rejects (→ `rejected`, token refunded). When the worker's `.glb` lands, `ArPartRepository.attach_model` writes **`approved` directly — auto-approve**, and the seller's `is_ar_visible` flag is the customer-facing gate. `approved` is deliberately *not* renamed to `completed`: the buyer viewer and the denormalised cache key off it, and a rename would ripple through three repos and break shipped builds.
- **`products.ar_*` is a cache, not state.** `_refresh_primary` only ever writes `approved` or `none` onto the product row, projecting the primary visible+approved part so the catalogue can filter without a join. Read `product_ar_parts` for truth.
- **`ar_generation_count` on `products` is DEPRECATED** — superseded by the per-part `free_scan_used` + `ar_current_gen_billed` pair (0059), which is what makes "first scan per part free, rescans cost a token, a failed *billed* generation refunds exactly one" expressible. The old per-product counter could not express any of that.
- Generated `.glb`/`.usdz` files are re-hosted server-side in the **`product-ar-models`** R2 bucket; the raw 3-photo scan **input** goes to the separate public **`product-ar-scans`** bucket.

### Support chat, AI designer, sets, views *(v1.6 — previously undocumented)*

- **`support_chats`** / **`support_messages`** (migration **0045**) — the customer ↔ platform support thread. **Not** the per-order chat: `support_chats.customer_id` is UNIQUE (one thread per user, no `order_id`), with `last_message_at`/`last_message_preview` and both unread counters (`customer_unread_count`, `admin_unread_count`). `support_messages` carries `sender_type` (`user|admin`), `message_type` (`text|image|audio` — **audio is why the app depends on `record` + `just_audio`**), `text_content` (≤4000) and/or `file_url`, `read_at`. A CHECK enforces the payload matches the type. Served by `/support/*` (customer) and `/admin/support/*`.
- **`ai_chat_logs`** (migration **0049**) — the AI Interior Designer transcript: `user_id` (FK, `ON DELETE SET NULL` so a deleted account leaves the BI row), `user_message`, `has_image`, `ai_response`, `created_at`. Read-only BI surface at `/admin/ai`, **owner-only** (it is a window onto customer messages).
- **`product_sets`** / **`set_items`** (migration **0057**) — curated multi-product sets (garnitur) a shop assembles from existing listings. `product_sets`: `shop_id`, `seller_id`, `name` + `name_i18n jsonb`, `description`, `cover_image_url`, `status` ∈ `draft|pending_review|approved|rejected|archived` (the `ProductStatus` value set), `rejection_reason`. `set_items`: `(set_id, product_id)` UNIQUE + `sort_order`, with a dedicated `product_id` index because the public catalogue projection runs a correlated subquery on it for every feed/search page. Moderated at `/admin/sets` (the `products` scope).
- **`product_views`** (migration **0080**) — one row per product impression: `product_id`, nullable `viewer_id` (SET NULL), `source` ∈ `detail|search|set`, `viewed_at`. It grows daily and must **never** be aggregated inline.
- **`product_view_counts_7d`** (materialized view, migration **0100**) — the 7-day rollup the personalized home feed's popularity term reads. Aggregating `product_views` directly was a sequential scan of every impression on every home-screen open. `ProductViewRollupSweeper` keeps it fresh with `REFRESH MATERIALIZED VIEW CONCURRENTLY`, which **requires the unique index** — do not drop it.

### AR tokens, deposits & achievements *(v1.6 — previously undocumented)*

- **`ar_token_transactions`** (migration **0039**) — append-only signed ledger for the seller's AR-scan credits, mirroring the wallet's shape: `seller_id`, `delta` (signed), `balance_after` (denormalised), `kind` ∈ `purchase|usage|refund|adjustment`, nullable `product_id` (set on usage/refund), `payment_id` (set on purchase), `note`.
- **`ar_token_purchases`** (migration **0053**) — the paid top-up request: `seller_id`, `package_code`, `tokens` (>0), `amount_uzs` (>0, whole so'm — the Payme link encodes ×100 as tiyin), `provider` ∈ `payme|click`, `status` ∈ `pending|paid|cancelled`. Moderated manually at `/admin/ar-tokens` (owner-only) when not settled by webhook.
- **`wallet_deposits`** (migration **0061**) — the *online* wallet top-up rail, distinct from the receipt-based `wallet_topups`: `seller_id`, `amount` (>0), `provider` ∈ `payme|click`, `status` ∈ `pending|paid|cancelled`, `paid_at`. The same migration adds `payme_transactions.wallet_deposit_id` and widens `target_kind` to the fourth value `wallet_deposit`.
- **`achievements`** / **`seller_achievements`** (migration **0015**) — the seller gamification layer. `achievements`: `code` UNIQUE, `title_uz`/`title_ru` + `description_uz`/`description_ru`, `icon`, `requirement_type` ∈ `orders_delivered|products_active|revenue_realized`, `threshold numeric`, `sort_order` (seeded in the same migration). `seller_achievements`: `(seller_id, achievement_id)` UNIQUE + `unlocked_at`. Read at `/seller/achievements` + `/seller/leaderboard`; managed at `/admin/achievements` — **the ninth delegable moderator scope** (see §6).

### Auth, verification history & QA *(v1.6 — previously undocumented)*

- **`otp_codes`** (migration **0002**) — `phone` PK (one row per number, replaced on each `/auth/otp/request` so a previous code is silently invalidated), `code_hash bytea` + **per-row `salt bytea`** (HMAC-SHA256; a leaked hash helps brute-force nothing else), `expires_at`, `attempts`, `last_request_at`. `last_request_at` is what makes the 60s per-phone cooldown an atomic claim rather than a read-then-write race.
- **`refresh_tokens`** (migration **0002**) — `jti` PK (carried in the refresh JWT), `user_id`, `issued_at`/`expires_at`, `revoked_at`. `revoked_at` covers two cases: explicit logout, and **rotation reuse-detection** — presenting a refresh token whose row is already revoked invalidates the user's whole chain. The same migration adds `profiles.phone_verified_at` + `app_metadata_role` and the partial-unique `profiles_phone_unique_idx` — which answers the §9 open question about where those columns came from.
- **`seller_verification_submissions`** (migration **0007**) — one row per verification *attempt*, so a rejected seller's history survives a resubmit. Snapshots the business fields (`business_type`, `legal_name`, `contact_phone`/`contact_email`, `telegram_username`), the shop fields (`shop_name`, `shop_description`, `shop_address`, `latitude`/`longitude`), the `documents jsonb` array sent in *that* attempt, plus `status`, `rejection_reason`, `submitted_at`/`decided_at`. Distinct from `seller_verifications`, which holds only the current state.
- **`qa_test_login_phones`** (migration **0098**) — `phone` (UNIQUE), `otp_code` (4–10 chars), `label`. A row here mints a **permanent OTP bypass** for that number, for store-review and QA accounts. That is exactly why `/admin/test-login-phones` is owner-only and `test-login-phones` is **not** a delegable moderator scope.
- **`ai_suggest_usage`** (migration **0102**) — per-caller daily quota for the vision prefill: `(user_id, day)` PK, `used` (CHECK ≥ 0), timestamps. `AiSuggestQuotaRepository.claim` is a single upsert whose `WHERE used < limit` on the ON CONFLICT branch makes a concurrent double-spend impossible; `refund` returns the call when the model never ran. **The day is Asia/Tashkent**, not the server's, so the cap does not reset in the middle of a seller's evening. `AI_SUGGEST_DAILY_LIMIT` defaults to 50; the super_admin **assisted** endpoint is deliberately exempt (drafting a recruited seller's 300-product catalogue would exhaust 50 in an afternoon).

---

## 6. API Contracts

### Base URL & versioning

- Production base: **`https://api.woody.uz`**. Every router is mounted under the **`/api/v1`** prefix (`app.include_router(api_router, prefix="/api/v1")`).
- OpenAPI/docs: `/api/v1/openapi.json`, `/api/v1/docs`, `/api/v1/redoc`.
- All handlers are `async def`.

### Authentication

- Scheme: **own JWT, HS256** (PyJWT). Clients send `Authorization: Bearer <access_token>` on every protected request. No external auth provider.
- Token payload: `{sub, phone, role, permissions, token_type, jti, iat, exp, iss, aud}`. `iss`/`aud` are verified on decode; `token_type` separates **access** vs **refresh** (an access token replayed at `/auth/refresh` is rejected).
- Login is phone + OTP (Eskiz SMS), not password:
  - `POST /api/v1/auth/otp/request` `{phone}` → `{sent, cooldown_seconds}`. Rate-limited.
  - `POST /api/v1/auth/otp/verify` `{phone, code}` → `TokenPair` `{access_token, refresh_token, token_type:"bearer", expires_in}`.
  - `POST /api/v1/auth/refresh` `{refresh_token}` → new `TokenPair`.
  - `POST /api/v1/auth/logout` `{refresh_token}` → 204.
- **Effective role is recomputed per request** (`get_effective_role`), never trusted from the JWT claim. Roles: `super_admin` (only the `SUPER_ADMIN_PHONE`) and `manager` (with optional delegable moderator scopes). Guards: public (none), `require_user` (any signed-in mobile user → 401), `require_admin` (any admin + path-derived scope check → 401/403), `require_role("super_admin")` (specific role).
- `GET /api/v1/me` is the source of truth for the resolved role + fresh permissions + push prefs (`promo_push_enabled`, `order_push_enabled`); `PATCH /me` accepts those booleans alongside profile fields; `DELETE /me` manages soft-delete (blocked on active orders / unsettled wallet).

### Standard request headers

- `Authorization: Bearer <token>` on every protected endpoint.
- **`Accept-Language`** — drives dynamic-content localization. Resolved by `parse_accept_language` to one of **`uz | ru | en`** (default/fallback `uz`). Accepts both the bare code the Flutter app sends (`ru`) and full browser-style values (`ru-RU,ru;q=0.9,en-US;q=0.8`), picking the highest-q supported subtag. Used to localize catalog category/attribute labels, cancellation reasons, OTP SMS template selection, etc. An unsupported header never 4xxs — it silently falls back to `uz`.
- `Content-Type: application/json` for JSON bodies.

### Route namespaces

- **Unauthenticated:** `GET /health`, `GET /health/ready`; `/auth/*`; **`GET /legal/oferta?lang=`** (seller Oferta body; uz fallback); `/openapi.json`, `/docs`; **`POST /webhooks/payme`** (Payme Merchant JSON-RPC, HTTP Basic — not JWT).
- **`/catalog/*`** (public storefront read): `/catalog/banners`, `/catalog/categories`, `/catalog/attributes`, `/catalog/products` (filtered list), `/catalog/products/{id}`, `/catalog/products/{id}/similar`, `/catalog/shops/{shop_id}`, `/catalog/news`, `/catalog/settings/{key}` (includes `payment_methods` for Payme/Click visibility).
- **Customer (signed-in, no explicit prefix on the router):** `/cart/items` (GET/POST/PATCH/DELETE), `/cart` (DELETE), `/favorites` (GET/POST/DELETE), `/orders` (list), `/orders/cancel-reasons`, `/orders/{id}`, `/orders/quote`, `POST /orders`, `/orders/{id}/cancel`, **`POST /orders/{id}/pay/{payme|click}`** (mint checkout URL once `awaiting_payment`), `/reviews` (GET/POST), `/push/device-tokens`.
- **`/chat`-area (signed-in):** `/chats`, `/chats/{id}/messages`, `/chats/{id}/read`, plus the user notification inbox `/notifications`, `/notifications/{id}/read`, `/notifications/read-all`.
- **`/seller/*`** (seller bearer, shop-scoped): `/seller/me`, `/seller/shop`, `/seller/products` (CRUD + `/archive`, `/restore`, `/{id}/ar-scan/photos`, `/ai-suggest`), `/seller/orders` (+ `/{id}/status`, `/{id}/delivery-fee`, `/{id}/accept`), `/seller/dashboard`, `/seller/achievements`, `/seller/leaderboard`, `/seller/analytics`, `/seller/reviews`, `/seller/tariff/*` (plans, current, receipts, history, **`/buy`** — mints the Payme/Click checkout link; there is no `/seller/tariff/pay` — `/payment-instructions`, receipt status), `/seller/wallet` (+ `/transactions`, `/topups`, **`/deposit`**), `/seller/ar-tokens/*` (**`/buy`**, purchase status), `/seller/services`, `/seller/attributes`, `/seller/verification/documents`.
- **`/storage/*`** — user-facing presigned upload helpers; **`/admin/storage/*`** — admin presigned PUT/GET (R2).
- **`/admin/*`** (admin panel). **Not every admin area is a delegable moderator scope** — an earlier revision of this spec said so and it was wrong. `MODERATOR_SCOPES` (`app/security/rbac.py`) holds exactly **nine**: `orders`, `products`, `sellers`, `shops`, `customers`, `reviews`, `categories`, `analytics`, `achievements`. Everything else under `/admin/` is either owner-only or deliberately ungated:
  - **Delegable (the nine scopes):** `/admin/orders`, `/admin/products` (+ `/admin/sets` and `/admin/ar-requests`, which both fold into the `products` scope), `/admin/sellers` (+ the assisted-onboarding routes, which hang off the same segment), `/admin/shops`, `/admin/customers`, `/admin/categories` (+ `/admin/subcategories`), `/admin/reviews`, `/admin/analytics`, `/admin/achievements`.
  - **Owner-only (`super_admin` alone; a scoped moderator 403s):** `/admin/settings`, `/admin/managers`, `/admin/tariffs`, `/admin/wallets`, `/admin/banners`, `/admin/news`, `/admin/notifications`, `/admin/support`, `/admin/ai`, `/admin/ar-tokens`, `/admin/test-login-phones`, and `/admin/legal/documents/{type}` (Oferta upsert per `lang`).
  - **Deliberately ungated** (any authenticated admin, including a scoped moderator): `/admin/dashboard`, `/admin/storage`, `/admin/alerts` — the explicit `_UNGATED_ADMIN_SEGMENTS` allowlist.
  - Enforcement is **centralized in `require_admin`** (`app/deps.py`): it derives the scope from the request path via `scope_for_admin_path` and checks `has_permission`, so every admin router is gated without per-router code. A segment that is neither scope-mapped nor allowlisted resolves to the `UNKNOWN_SCOPE` sentinel and **fails closed**.

- **Admin routers this spec had never listed** *(v1.6)*: **`/support`** + **`/admin/support`** (customer↔platform support chat, §5), **`/ai`** + **`/admin/ai`** (AI Interior Designer, and the owner-only log/BI view), **`/admin/sellers/*/assisted`** (concierge onboarding — see below), **`/admin/ar-requests`** (the pre-generation 3D queue), **`/admin/ar-tokens`** (manual AR-token purchase moderation), **`/admin/sets`** (curated product sets), **`/admin/alerts`** (the operational alert bell), **`/admin/test-login-phones`** (QA OTP-bypass numbers).

- **`/admin/settings`** exposes seven typed keys on top of the generic `GET|PUT /admin/settings/{key}` pair, each backed by one `app_settings` row: `receiving-card`, `app-versions`, `maintenance`, `support-contacts`, `payment-methods` (the Payme/Click visibility switch the app reads via `/catalog/settings/payment_methods`), `demo-models`, `master-otp`.

- **Assisted onboarding** — `/admin/sellers/assisted` (create the drafted account + shop, idempotent by phone), `GET|POST /admin/sellers/{id}/assisted/products` (drafts land in **`draft`**, not `pending_review`), `DELETE .../products/{pid}`, `POST .../assisted/publish`, and `POST /admin/sellers/assisted/ai-suggest` (the same vision service as the seller endpoint, re-exposed behind the owner gate and **unquota'd**). **super_admin only.** The interlock that makes this safe: nothing here writes `sellers.contract_accepted_at` — only the seller's own `POST /seller/onboarding` does — and both `publish_drafts` and `SellerWriteRepository.approve` raise `SellerConsentRequired` → **409 `consent_required`** while it is null. The routes add no new `/admin/` segment (they hang off `/admin/sellers`), so the RBAC mirror is untouched.
- **`/realtime/*`** + WebSocket endpoints for live chat/notification updates; **`/internal/ar-webhook`** is the alternate AR-completion callback.

### Pagination conventions

- **Offset/limit** is the norm (no opaque cursors). List endpoints take `limit: int = Query(..., ge=1, le=<max>)` and `offset: int = Query(0, ge=0)`. Defaults vary by surface — e.g. catalog products default `limit=15` (max 200), customer orders default `limit=20` (max 100), similar products default `limit=10` (max 50). The rule card baseline is `limit=20, le=100`.
- List responses that need it return a wrapper with a `total` count alongside the rows (e.g. `CatalogProductList {items, total}`, `CustomerOrderList {items, total}`); the client paginates by advancing `offset`. Simpler lists return a bare array.
- Catalog product filtering is rich: `category_id`, `subcategory_id`, `shop_id`, `color[]`, `min_price`/`max_price`, `in_stock`, `has_delivery`, `discounted`, `search`, `sort` (e.g. `newest`).

### Error response shape

- Errors use FastAPI's default JSON body: **`{"detail": "<code-or-message>"}`**. Domain services raise typed exceptions that routers translate via a `_translate(exc)` helper to stable string codes:
  - `422 invalid_phone`, `429 rate_limited` (with **`Retry-After`** header), `410 otp_expired`, `429 otp_attempts_exhausted`, `401 invalid_code`, `401 invalid_refresh_token`, `403 account_blocked`.
  - Auth/permission gates: `401 not_authenticated`, `403 insufficient_role`, `403 insufficient_permission`.
  - Feature gates: `409 consent_required` (publishing or approving an admin-drafted seller who has not accepted the Oferta), `503 ar_pipeline_unavailable` (Meshy unconfigured/disabled — the app falls back to the manual flow).
  - **`/seller/products/ai-suggest` and its admin twin never return an error** — they are always `200`. Failure is expressed in the body: `available:false` plus an `unavailable_reason` of `disabled` / `daily_limit` / `invalid_images` / `failed`, with `daily_remaining` carrying the quota balance. An exception there would crash the seller's form, so graceful degrade is the contract, not a convenience.
- Validation failures (pydantic) return FastAPI's standard `422` with a `detail` array of field errors.
- Non-domain failures (DB down, R2 5xx) are not wrapped — they propagate to FastAPI's default `500` handler.


---

## 7. Technical Debt Register

> ⚠️ **This whole section is a 2026-06-18 snapshot.** All 25 findings in it were resolved by that date and nothing has been re-audited since — roughly **three months of subsequent code** (the Payme webhook, per-part AR, product sets, support chat, the AI Interior Designer, catalogue performance work, assisted onboarding, the AI quota) is **not covered by any finding below**. Read it as closed history, not as current coverage. The only live debt register is `mebellar_app/doc/planning/tech_debt_roadmap.md`, and it covers the Flutter app alone.

This register is the output of a workspace-wide audit (2026-06-18): four parallel per-repo code audits, with **every finding independently re-verified against the source by a separate skeptical reviewer**. Of 29 raised findings, **25 were confirmed** and **4 were rejected as false positives** (the verification layer working as intended). Severities below are the **post-verification adjusted** values. Full descriptions, evidence, and per-finding fixes: [`_archive/workspace_audit_2026_06_18.md`](_archive/workspace_audit_2026_06_18.md) (archived — all 25 resolved).

### Summary by area × severity (confirmed only)

| Area | High | Medium | Low | Total |
|---|---:|---:|---:|---:|
| woody_backend | 0 | 1 | 5 | 6 |
| mebellar_app (Flutter) | 0 | 1 | 4 | 5 |
| woody_admin | 1 | 3 | 3 | 7 |
| woody_frontend | 2 | 1 | 4 | 7 |
| **Total** | **3** | **6** | **16** | **25** |

> **Reassuring context:** the deep money/auth/storage paths audited cleanest — the OTP/refresh TOCTOUs (SEC-01/02/04), the wallet/commission idempotency, the one-shop-per-order IDOR guard, and storage object-ownership binding are all **confirmed correctly implemented**. The debt is concentrated in client-edge robustness (admin SSR auth, marketing-site polish) rather than in the core transactional engine.

> **Progress (2026-06-18):** **all 25 confirmed findings are now resolved** — 6 backend (BE-01 – BE-06), 5 mebellar_app (OTP-01, CHAT-01, OTP-02, TOK-01, CART-01), 7 woody_admin (AUTH-01, SEC-01, DATA-01, UX-01, SEC-02, RBAC-01, DATA-02), and 7 woody_frontend (FE-01 – FE-07) — see *Resolved (post-audit)* at the end of this section. The counts/table above are the original audit snapshot (confirmed-at-audit-time), intentionally left unchanged as the historical baseline; **0 confirmed findings remain open.**

### Critical / High — fix before / right after next release

| ID | Area | Title | Recommended fix |
|---|---|---|---|
| ~~AUTH-01~~ | admin | ✅ **RESOLVED (2026-06-18)** — refresh moved out of the RSC render path into the `proxy.ts` edge middleware (sets the rotated pair on request + response); `apiFetch` no longer writes cookies during render. See *Resolved (post-audit)*. | — |
| ~~FE-01~~ | frontend | ✅ **RESOLVED (2026-06-18)** — removed `espresso`/`cream`/`clay` scales re-added to `tailwind.config.ts` as aliases (all used shades covered), so the un-migrated files render as designed. See *Resolved (post-audit)*. | — |
| ~~FE-02~~ | frontend | ✅ **RESOLVED (2026-06-18)** — AASA appID now built from `process.env.APPLE_TEAM_ID` (real Team ID, not `TEAMID`) with a 10-char validation guard that fails the build on a placeholder. See *Resolved (post-audit)*. | — |

### Medium — schedule into the next 1–2 sprints

| ID | Area | Title | Recommended fix |
|---|---|---|---|
| ~~BE-01~~ | backend | ✅ **RESOLVED 2026-06-18** — Eskiz SMS failure surfaced as a raw 500 and the cooldown was committed before the send. See *Resolved (post-audit)* at the end of this section. | — |
| ~~OTP-01~~ | flutter | ✅ **RESOLVED 2026-06-18** — Android SMS autofill now re-arms on resend: the prior `smart_auth` listener is torn down and a `_smsListenGeneration` token guards the re-arm. See *Resolved (post-audit)* at the end of this section. | — |
| ~~SEC-01~~ | admin | ✅ **RESOLVED 2026-06-18** — `next.config.ts` now ships an enforced CSP (R2/woody.uz + API origin allowed; `frame-ancestors 'none'`; `worker-src blob:`) + `X-Frame-Options: DENY`, HSTS, `nosniff`, `Referrer-Policy`. See *Resolved (post-audit)*. | — |
| ~~DATA-01~~ | admin | ✅ **RESOLVED 2026-06-18** — dashboard runs ONE aggregation via `getDashboardOverview(...)` returning `{kpis, recentOrders}`. See *Resolved (post-audit)*. | — |
| ~~UX-01~~ | admin | ✅ **RESOLVED 2026-06-18** — shared `Pagination` component + `page`/`offset` searchParam wired into orders, products, customers, sellers (filters preserved). See *Resolved (post-audit)*. | — |
| ~~FE-03~~ | frontend | ✅ **RESOLVED 2026-06-18** — added `app/[locale]/shop/[id]/page.tsx` (download-CTA landing mirroring product) + `shop` dict keys in all 3 locales + `woody_shop_` deep-link token. See *Resolved (post-audit)*. | — |

### Low — opportunistic / hardening (16 confirmed)

| ID | Area | Title |
|---|---|---|
| ~~BE-02~~ | backend | ✅ **RESOLVED 2026-06-18** — admin scope gating now fails **closed**: unmapped `/admin/` segments → `UNKNOWN_SCOPE` (denied to scoped moderators) + explicit ungated allowlist + a coverage test. See *Resolved (post-audit)*. |
| ~~BE-03~~ | backend | ✅ **RESOLVED 2026-06-18** — AR-webhook secret now compared with `hmac.compare_digest`. See *Resolved (post-audit)*. |
| ~~BE-04~~ | backend | ✅ **RESOLVED 2026-06-18** — Meshy `.glb` download now streams with a 75 MB cap + content-type/length sanity checks before re-hosting. See *Resolved (post-audit)*. |
| ~~BE-05~~ | backend | ✅ **RESOLVED 2026-06-18** — `CartRepository.remove` rewritten to the explicit `return not tag.endswith(" 0")`. See *Resolved (post-audit)*. |
| ~~BE-06~~ | backend | ✅ **RESOLVED 2026-06-18** — `parse_permissions` docstring corrected (the pool *does* register a jsonb codec). See *Resolved (post-audit)*. |
| ~~CHAT-01~~ | flutter | ✅ **RESOLVED 2026-06-18** — bootstrap's resolved `Chat` passed into `ChatThreadCubit.load(initialChat)`, skipping the redundant list-scanning `getChat`. See *Resolved (post-audit)*. |
| ~~OTP-02~~ | flutter | ✅ **RESOLVED 2026-06-18** — PIN auto-submit / error-clear are now cancellable `Timer`s (cancelled in dispose + per `_handle`) with a one-shot `_lastSubmittedValue` guard. See *Resolved (post-audit)*. |
| ~~TOK-01~~ | flutter | ✅ **RESOLVED 2026-06-18** — `TokenStore.read()` hydration is now single-flight via a memoised `_hydration` future. See *Resolved (post-audit)*. |
| ~~CART-01~~ | flutter | ✅ **RESOLVED 2026-06-18** — guest-cart merge now parallel (`Future.wait`), keeps transient failures locally, drops only permanent ones, and reports a `CartMergeResult` via `mergeEvents`. See *Resolved (post-audit)*. |
| ~~SEC-02~~ | admin | ✅ **RESOLVED 2026-06-18** — `safeNext()` in `login/actions.ts` accepts only single-`/`-leading paths, rejecting `//` and `/\`. See *Resolved (post-audit)*. |
| ~~RBAC-01~~ | admin | ✅ **RESOLVED 2026-06-18** — the three mutation actions the audit named (`products`/`reviews`/`subcategories`) now use `requirePermission('products'|'reviews'|'categories')`. **The fix covered only those** — it was never a panel-wide sweep, and the admin has since grown to **43 pages** under `app/(admin)/` and **22 modules** in `lib/actions/`. The real gate is the backend's centralized `require_admin` scope check (§6), which all of them pass through; the in-process `requirePermission` calls are fast redirects. See *Resolved (post-audit)*. |
| ~~DATA-02~~ | admin | ✅ **RESOLVED 2026-06-18** — receipts page presigns only `pending` rows; history rows fetch their signed URL lazily via the guarded `getReceiptUrlAction`. See *Resolved (post-audit)*. |
| ~~FE-04~~ | frontend | ✅ **RESOLVED 2026-06-18** — `error.tsx` is self-contained: locale from `usePathname()` + inline COPY map instead of `useI18n()`. See *Resolved (post-audit)*. |
| ~~FE-05~~ | frontend | ✅ **RESOLVED 2026-06-18** — added `scripts/translation-check.ts` + `translation:check` npm script (fails on ru/en leaves identical to uz; brand allowlist); fixed `home.app.title`. See *Resolved (post-audit)*. |
| ~~FE-06~~ | frontend | ✅ **RESOLVED 2026-06-18** — `next.config.mjs` CSP flipped from Report-Only to enforcing `Content-Security-Policy`. See *Resolved (post-audit)*. |
| ~~FE-07~~ | frontend | ✅ **RESOLVED 2026-06-18** — the three `public/mockups/*.png` downscaled in place via `sips` (~70% smaller, same filenames). See *Resolved (post-audit)*. |

### Architectural anti-patterns (cross-cutting themes)

- **SSR auth side-effects (AUTH-01).** Mutating cookies during a React Server Component render is structurally fragile in the App Router; refresh belongs in middleware / route handlers / server actions. This is the single highest-leverage structural fix.
- **Eager / duplicated data fetching (DATA-01, DATA-02).** Same heavy aggregation fetched twice; presigning every row regardless of use. Adopt `cache()` + lazy/on-demand fetching as a repo convention.
- **Unbounded list rendering (UX-01).** No pagination on admin lists is a correctness bug at scale, not just UX — the backend already supports offset/limit, so this is purely a frontend gap.
- **Incomplete refactors shipped (FE-01).** A color-palette migration was merged half-done. Add CI guards (grep for removed token names) so a partial refactor fails the build.
- **Type system used for shape, not content (FE-05, and the Flutter dual-hash / enum mirrors).** Several cross-surface contracts (trilingual dictionaries, status enums, the OTP signature hash, RBAC scope lists) are hand-mirrored across repos and only shape-checked. These need parity tests, not just types.
- **Best-effort sequential loops (CART-01).** Guest-cart merge awaits per item and swallows failures; prefer batch endpoints + explicit partial-failure reporting.

### Verification integrity note

The 4 findings rejected during verification (mebellar_app `NOTIF-01`, `RT-01`, `PUSH-01`, `API-01`) were each traced to a real safeguard already in the code (single-flight refresh dedup, `str()`-wrapped UUIDs on the wire, mode-filtered seller bell, no data-only pushes sent). They are recorded in the audit doc as "watch items," not debt.

### ✅ Resolved (post-audit)

- **BE-01** (backend, Medium) — *Eskiz SMS failure → raw 500 + cooldown burned* — **fixed 2026-06-18.** Both defects closed:
  - `EskizClient` now wraps the two `httpx.post` calls (`_post_sms`, `_login`) so a transport error/timeout surfaces as `EskizError`, never a raw `httpx.TimeoutException` (`app/clients/eskiz.py`).
  - `OtpService.request_otp` keeps the atomic SEC-02 cooldown claim **before** the send (preserving the concurrent-double-SMS guard) but now wraps `send_sms` in `try/except EskizError`: on failure it **rolls the claim back** (`otp_codes` row deleted) and raises the new typed `SmsDispatchFailed(AuthError)`. A failed dispatch no longer consumes the cooldown — the user can retry immediately.
  - The router translates `SmsDispatchFailed` → **HTTP 502 `sms_unavailable`** (no `Retry-After`, since retry is immediate), so an Eskiz outage returns a clean typed error instead of a 500 (`app/api/v1/auth.py`).
  - The narrow `except EskizError` is deliberate: per CLAUDE.md, genuine non-domain errors still propagate to FastAPI's 500 handler (a regression test locks this boundary so a future widening to `except Exception` fails CI).
  - Tests: `tests/unit/test_otp_service.py` (rollback, immediate-retry, non-EskizError still 500-class + no rollback), `tests/test_auth_endpoints.py` (502 + retry), `tests/unit/test_eskiz.py` (httpx error wrapped). Full suite green.

- **BE-02 – BE-06** (backend, all Low) — *the five remaining backend hardening items* — **fixed 2026-06-18** (`app/security/rbac.py`, `app/api/v1/ar_webhook.py`, `app/clients/meshy.py`, `app/services/ar_pipeline.py`, `app/services/customer_repos.py`):
  - **BE-02 (scope gating fail-open).** `scope_for_admin_path()` now fails **closed**: an unmapped `/admin/` segment returns the new `UNKNOWN_SCOPE` sentinel (not `None`), which `has_permission()` denies to scoped moderators (super_admin + full managers still pass). Deliberately-ungated segments are an explicit allowlist — **`_UNGATED_ADMIN_SEGMENTS = {dashboard, storage, alerts}`** as of v1.6 (`alerts`, the operational alert bell, was added to the allowlist after the audit; earlier revisions of this spec listed only the first two). A new `test_rbac.py` coverage test asserts every registered `/admin/` router segment is mapped or whitelisted, so a future router with a forgotten `_PATH_SCOPE` entry fails CI instead of silently leaking.
  - **BE-03 (timing side-channel).** The AR-webhook secret check now uses `hmac.compare_digest(x_ar_webhook_secret or "", expected)` — constant-time, matching the OTP idiom.
  - **BE-04 (unbounded `.glb` download).** `download_glb()` now **streams** with a 75 MB ceiling (over-cap `Content-Length` rejected pre-read, per-chunk running-total guard, non-binary `Content-Type` refused); the AR worker also guards against re-hosting a 0-byte body. New unit tests in `test_meshy_client.py` + `test_ar_pipeline.py`.
  - **BE-05 (precedence trickery).** `CartRepository.remove` rewritten to the explicit `return not tag.endswith(" 0")` with a comment on asyncpg's `"DELETE <n>"` tag format.
  - **BE-06 (stale docstring).** `parse_permissions` docstring corrected — the pool *does* register a json/jsonb codec (`database.py`), and the string branch is documented as defensive for connections without that init hook.
  - Full backend suite green (825 passed); changed files `ruff`-clean.

- **OTP-01 / CHAT-01 / OTP-02 / TOK-01 / CART-01** (mebellar_app — OTP-01 Medium, the other four Low) — *all five confirmed Flutter findings* — **fixed 2026-06-18** (`lib/auth/auth_sheet_controller.dart`, `lib/shared/chat/bloc/chat_thread_cubit.dart` + `chat_thread_screen.dart`, `lib/auth/sheets/otp_step.dart`, `lib/core/network/token_store.dart`, `lib/shared/repositories/hybrid_cart_repository.dart`):
  - **OTP-01 (autofill never re-arms on resend).** `_listenForSmsCode()` drops the `_smsListenerActive` early-return: it now tears down any prior `smart_auth` listener (`removeSmsRetrieverApiListener()` + `removeUserConsentApiListener()`) before re-arming, and a monotonic `_smsListenGeneration` token makes a superseded in-flight listen bow out so a stale result/teardown can't clobber the fresh one. Off-Android stays a no-op, so the existing controller tests are unaffected.
  - **CHAT-01 (double chat-list fetch on open).** The bootstrap `FutureBuilder`'s already-resolved `Chat` is passed into `ChatThreadCubit.load(initialChat)` (`..load(widget.chat)`), so `load()` skips the list-scanning `getChat` and seeds the row straight into the loading state; the retry path reuses `state.chat`.
  - **OTP-02 (uncancelled auto-submit timers).** The 120ms submit and 500ms error-clear `Future.delayed` calls are now cancellable `Timer`s (cancelled in `dispose()`; the submit timer is also cancelled at the top of each `_handle`), with a `_lastSubmittedValue` one-shot guard so each completed code fires `onCompleted` exactly once (reset on error-clear so a legitimate re-submit still works).
  - **TOK-01 (non-atomic hydration).** `read()` is now single-flight — cache when `_hydrated`, else a memoised `_hydration ??= _hydrate()` so a cold-start burst shares one storage round-trip; `_hydrate()` defers to a `write()`/`clear()` that lands mid-flight rather than overwriting it with stale reads.
  - **CART-01 (sequential swallow-all merge).** `_mergeGuestCart` replays lines in parallel via `Future.wait`, distinguishes transient (kept locally to retry) vs permanent (dropped) failures, removes from the local Hive cart only merged + permanently-dropped lines, and emits `CartMergeResult{merged, dropped}` on a new `mergeEvents` stream so the UI can surface lost items.
  - Tests: new `test/core/network/token_store_test.dart` + `test/auth/sheets/otp_step_test.dart`, plus added cases in `chat_thread_cubit_test.dart` and `hybrid_cart_repository_test.dart`. Full Flutter suite green (596 passed); the six changed `lib/` files are `dart analyze`- and `dart format`-clean.

- **AUTH-01 / SEC-01 / DATA-01 / UX-01 / SEC-02 / RBAC-01 / DATA-02** (woody_admin — AUTH-01 High, SEC-01/DATA-01/UX-01 Medium, rest Low) — *all seven confirmed admin findings* — **resolved by 2026-06-18** (`next.config.ts`, `proxy.ts`, `lib/api/client.ts`, `lib/queries/dashboard.ts`, `app/(admin)/page.tsx`, `components/admin/pagination.tsx` + the four list pages, `app/login/actions.ts`, `lib/actions/{products,reviews,subcategories,tariff}.ts`, `app/(admin)/tariffs/receipts/page.tsx`, `components/admin/lazy-receipt-link.tsx`):
  - **AUTH-01 (High; prior session).** Refresh moved to the `proxy.ts` edge middleware — when the access cookie is gone but the refresh cookie remains it refreshes and sets the rotated pair on both the forwarded request and the response. `apiFetch` no longer writes cookies during render; a 401 → null session → `requireAdmin()` redirect.
  - **SEC-01 (Medium).** `next.config.ts` `headers()` ships an enforced CSP (`default-src 'self'`; `'unsafe-inline'` for script/style, `'unsafe-eval'` dev-only; R2/woody.uz wildcards + API origin on img/connect; `worker-src 'self' blob:` for model-viewer; `frame-ancestors 'none'`; `object-src 'none'`) + `X-Frame-Options: DENY`, HSTS (`max-age=63072000; includeSubDomains`), `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`.
  - **DATA-01 (Medium).** New `getDashboardOverview(n)` returns `{kpis, recentOrders}` from one `/admin/dashboard` aggregation; the page destructures both (was two). *(v1.6: the call site is `getDashboardOverview(6)` — `app/(admin)/page.tsx` — not `8`; the argument is the recent-orders row count and was changed after the audit.)*
  - **UX-01 (Medium).** New shared `components/admin/pagination.tsx` + `page`/`offset` driven from searchParams across orders/products/customers/sellers, preserving filters.
  - **SEC-02 (Low).** `safeNext()` accepts only single-`/`-leading paths (rejects `//`, `/\`), closing the post-login open redirect.
  - **RBAC-01 (Low).** `setProductStatusAction`, `deleteReviewAction`, and the three subcategory actions now use `requirePermission('products'|'reviews'|'categories')`. *(v1.6 clarification: this was a five-action fix, not an audit of the whole panel. Measured 2026-09-18, the admin has 43 `page.tsx` files under `app/(admin)/` and 22 action modules; a claim elsewhere that RBAC-01 covered "3 mutation actions" understates it, and a claim that it covered the panel overstates it. Client-side coverage is not the security boundary either way — the backend re-checks every request.)*
  - **DATA-02 (Low).** Receipts page eagerly presigns only `pending` rows; history rows fetch their URL on click via the super_admin-guarded `getReceiptUrlAction`.
  - Verified: `tsc --noEmit` clean, `next build` green (full route table), ESLint clean on all changed files (only pre-existing `.claude/helpers` tooling warnings remain).

- **FE-01 – FE-07** (woody_frontend — FE-01/FE-02 High, FE-03 Medium, rest Low) — *all seven confirmed frontend findings* — **resolved by 2026-06-18** (`tailwind.config.ts`, `app/.well-known/apple-app-site-association/route.ts`, `app/[locale]/shop/[id]/page.tsx`, `lib/i18n/dictionaries.ts`, `components/blocks/download-app-cta.tsx`, `app/[locale]/error.tsx`, `scripts/translation-check.ts`, `next.config.mjs`, `public/mockups/*.png`):
  - **FE-01 (High; prior session).** Legacy `espresso`/`cream`/`clay` scales re-added to `tailwind.config.ts` as aliases (all referenced shades covered) — the ~18 un-migrated files render as designed.
  - **FE-02 (High; prior session).** AASA appID built from `process.env.APPLE_TEAM_ID` (real 10-char Team ID, default `LQ278UVP2Y`) with a regex guard that throws on a placeholder.
  - **FE-03 (Medium).** Added `app/[locale]/shop/[id]/page.tsx` (download-CTA landing mirroring the product page) + `meta.pages.shop` and a `shop` copy block in all three locales + a `kind='shop'` `download-app-cta` variant emitting the `woody_shop_<id>` deep-link token.
  - **FE-04 (Low).** `error.tsx` is self-contained — locale from `usePathname()`, inline per-locale COPY map, no `useI18n()`.
  - **FE-05 (Low).** New `scripts/translation-check.ts` + `translation:check` npm script (Node `--experimental-strip-types`, no new deps) fails on any ru/en leaf identical to uz (brand allowlist + structural-key skips); fixed the one real miss (`home.app.title`).
  - **FE-06 (Low).** `next.config.mjs` CSP flipped from `Content-Security-Policy-Report-Only` to enforcing `Content-Security-Policy` (bytes unchanged).
  - **FE-07 (Low).** The three `public/mockups/*.png` downscaled in place via `sips` (976→301, 849→270, 663→170 KB), same filenames so no references change.
  - Verified: `tsc --noEmit` clean, `next lint` clean, `next build` green (shop route present), `translation:check` green.

---

## 8. Technical Roadmap (6–12 months)

A pragmatic, phased plan. Phase 0 is the debt above; Phases 1–3 scale the platform toward enterprise-grade.

### Phase 0 — Stabilize (weeks 0–4)

- Land the **3 High** debt items (AUTH-01 silent-logout, FE-01 broken styles, FE-02 iOS deep-link placeholder) and the **6 Medium** items.
- Flip pending go-lives that are code-complete but config-gated (per repo memory/notes): **FCM iOS APNs**, **Eskiz template approvals** (all 3 locales matching the signing-key hash), **AR Meshy key + `AR_PIPELINE_ENABLED`**, **AI deployment**, and **admin CSP enforce**.
- ~~Reconcile the **deploy-doc drift** (systemd vs Docker for backend/admin) so the runbook matches reality.~~ ✅ **DONE (v1.6, 2026-09-18)** — all three server surfaces confirmed Docker Compose against the live workflows; §2 rewritten, and the stale `npm ci`/systemd copy in `woody_admin/CLAUDE.md` (the source the drift was copied from) corrected at the same time.

### Phase 1 — Scalability & Performance (months 1–3)

- **Caching layer.** Introduce Redis (already an optional dependency) as a read cache for the hot public catalog paths (`/catalog/products`, categories, attributes, banners) with short TTL + tag invalidation on writes. The catalog is read-dominant and the biggest scaling lever.
- **Database.** Audit indexes against the live slow-query log (the audit confirmed good coverage on current hot paths; validate under real traffic). Add **cursor (keyset) pagination** for the largest tables (orders, products, wallet_transactions) — offset/limit degrades on deep pages. Plan a **read replica** + pooler (PgBouncer) before the single primary saturates; right-size the asyncpg pool per worker.
- **Backend throughput.** Confirm uvicorn worker count vs CPU; ensure no remaining sync I/O on the event loop (BE-04 streaming fix folds in here). Add request-level timeouts/circuit-breakers around Eskiz/Meshy/Azure so a slow provider can't exhaust workers.
- **Flutter performance.** Enable and validate **Impeller** (default on recent Flutter for iOS; verify Android), profile jank on the home feed and chat lists, audit `const`/keys on long lists, and confirm `cached_network_image` eviction policy. Ship image **AVIF/WebP** from the pipeline (also FE-07).

### Phase 2 — DevOps & CI/CD (months 2–5, parallel)

- **Single source-controlled workspace.** Each of the four modules is its own git repo, checked out side by side; **nothing versions the set of them together**. *(v1.6 correction: this spec IS tracked — it lives at `mebellar_app/doc/TZ.md` and rides that repo's history. The earlier claim that "root `TZ.md` isn't tracked" described a workspace-root file that does not exist.)* The gap that remains is real: a platform-level change to a hand-mirrored contract (§Appendix) still lands as N independent commits in N repos with no single review. Establish a top-level repo (or git submodules / a monorepo tool) so cross-repo contracts are versioned and reviewable as one change.
- **Test gates in CI.** Make every deploy pipeline **block on tests** — backend `pytest` (726+ passing), admin/frontend `tsc --noEmit` + `eslint` + `next build`. Add the cross-repo **enum/RBAC/OTP-hash parity** checks as a dedicated CI job (these are hand-mirrored and the most likely silent drift) — host it in the **backend** pipeline, since the mobile repo has none. **The Flutter app is deliberately excluded from this item:** its GitHub Actions workflow was removed on 2026-09-17 after an unpinned `channel: stable` runner drifted ahead of the local SDK and reddened every push for four weeks. Its gate is local — `dart analyze lib/ test/` + `flutter test` (923 tests) before a commit. If it is ever restored, the Flutter version must be **pinned**.
- ~~**Containerize uniformly.**~~ ✅ **DONE — closed in v1.6.** Backend and admin both run under Docker Compose on the server, each with a deploy-time health check (backend: `/api/v1/docs` on 4001; admin: 200/302/307 on 3002 within 30s), so all three server surfaces are now containerized. **Two sub-items stay open** and move to the backlog: *pin base images* (the Dockerfiles still track floating tags) and *remove git-tracked tooling waste* (`.claude-flow/`, `.swarm/`, `ruvector.db`, build caches — `woody_frontend/CLAUDE.md` still lists these as tracked).
- **Observability.** Add backend error tracking (Sentry was deliberately removed from Flutter to avoid the `FlutterError.onError` clash — choose a backend-side sink), structured JSON logging with request IDs, and basic metrics/tracing (OpenTelemetry) on the FastAPI app. Mobile already has Crashlytics + Analytics, both **gated by the Settings privacy toggle** (and muted until Hive preference is applied at cold start).
- **Migrations discipline.** Keep the `woody migrate` discrete CI step; assert head consistency against the documented Alembic head — **`0102_ai_suggest_quota` as of 2026-09-17** (verified against `woody_backend/alembic/versions/`).

### Phase 3 — Feature Maturity / Enterprise-grade (months 4–12)

- **Click settlement webhook.** Payme Merchant API is **live** (orders, tariffs, AR tokens, wallet deposits + fiscalization; strict `awaiting_payment` gate; refund cancels the order). Click today only mints `my.click.uz` URLs — add Prepare/Complete (or equivalent) merchant callbacks so Click payments auto-confirm the same four targets.
- **Payment UX polish.** Failed/cancelled Payme outcomes in Flutter (`PaymentOutcome` today is only paid/pending/unknown); orders-list Pay CTA; optional payment-return deep link.
- **Realtime chat hardening.** Move from refresh-on-open fallback toward fully reliable WebSocket delivery at multi-worker scale (Redis pub/sub fan-out is in place — load-test it); add typing indicators / delivery guarantees.
- **AR pipeline to GA.** Once Meshy is live, add quotas, cost controls, and admin QC SLAs; cache/CDN the `.glb` assets.
- **Search & discovery.** Postgres full-text or a dedicated search service for the catalog as the product count grows; recommendation tuning (the feed already supports discount/popular/recommended sorts).
- **Trust & safety / ops tooling.** Admin audit log, bulk moderation, seller analytics depth, dispute/refund flows tied to the wallet ledger.
- **Internationalization at scale.** Move the hand-synced trilingual dictionaries (FE-05) and DB content translations toward a managed translation workflow with parity enforcement.

---

## 9. Open Questions & Uncertainties

Items the 2026-06-18 audit could not fully resolve from code alone — confirm before relying on them:

- ~~**Postgres version** is not pinned in any manifest.~~ **ANSWERED (v1.6):** production Postgres is **Azure Database for PostgreSQL**, a managed instance — which is why no repo manifest pins it, and why `CREATE EXTENSION` is gated behind the `azure.extensions` server parameter (see §2). The exact server version is an Azure portal setting, not a repo fact. **Alembic head** should still be verified against production (`woody current`); the repo head is **`0102_ai_suggest_quota`** — confirm the live DB matches. Migrations added since v1.4 and **not yet described in §5**: `0095` order payment window (`orders.payment_expires_at`, 30 min + expiring-push type), `0096`/`0097` demo-model seeds, `0098` QA test-login phones, `0099` `profiles.seller_promo_dismissed`, `0100` catalog performance (7-day view rollup + browse/search indexes), `0101` assisted onboarding (admin-drafted sellers + products), `0102` `ai_suggest_usage` per-user daily quota. Of these the Flutter app currently consumes only `0099`; `0101`/`0102` are backend/admin-side so far.
- ~~**Deploy mechanism** (systemd vs Docker for backend/admin) is documented inconsistently and was not verified against the actual GitHub Actions workflows.~~ **ANSWERED (v1.6):** verified against all three workflows — **every server surface is Docker Compose**, none is systemd. The backend migrates in a throwaway container *between* build and swap; the admin needs `--no-deps` to stay inside its 15-minute timeout. Full detail in §2.
- **Store presence** (iOS App Store live vs Android-only) is unconfirmed; `pubspec` configures both.
- **Integration go-live status**: Eskiz (all 3 templates approved?), FCM iOS APNs, Azure OpenAI deployment, Meshy key, and **Payme merchant cabinet** (account field names + fiscal MXIK codes for live products) are config-gated — confirm production credentials. Click settlement webhook is **not** built yet. Internal escrow + manual withdrawals are live in code (not Payme Safe/Split).
- **JWT access-token TTL** exact values live in settings/`jwt.py` (15 min user / 1h admin per code comments) — confirm against config.
- **`product_variants` / `product_images`** vs `products.images[]`: which the live app actually reads was inferred, not traced end-to-end.
- ~~**`app_metadata_role` / `app_metadata_permissions`** columns on `profiles` … the exact adding migration wasn't pinpointed.~~ **PARTLY ANSWERED (v1.6):** `app_metadata_role` is added by **`0002_auth_tables`** (alongside `phone_verified_at` and the partial-unique `profiles_phone_unique_idx`) — see §5. `app_metadata_permissions` (the jsonb scope list) arrives in a later revision; pin it when next in that file.
- **Trial-bonus zero-rate stamping path** (commission_rate=0 on `order_items` vs plan-level) was inferred from comments, not read directly.

---

## Appendix — Document Map & Cross-Repo Contracts

### Canonical documentation layout (post-consolidation, 2026-06-18)

> **Corrected in v1.6.** The map below used to describe a workspace-root `TZ.md` beside a workspace-root `docs/` folder. Neither exists — and the contradiction was visible six lines into this very file, whose header already says the spec lives at `mebellar_app/doc/TZ.md`. The real layout:

```
mebellar_app/
  doc/
    TZ.md                              ← THIS FILE — platform master spec, single source of truth
    architecture/system_design.md      ← detailed architecture & design
    planning/                          ← near-term execution (incl. tech_debt_roadmap.md)
    roadmap.md  backlog.md
    release_checklist.md  deploy_all.sh
    _archive/                          ← retired specs + the closed 2026-06-18 audit (all 25 resolved, behind §7)
  docs/                                ← per-topic mobile guides (ar, payments, ai-designer, env-config, …)
  CLAUDE.md  woody_mobile_tz.md  .claude/rules/*
woody_backend/  CLAUDE.md  woody_backend_tz.md  docs/  deploy/  .claude/rules/*
woody_admin/    CLAUDE.md  AGENTS.md   woody_admin_tz.md  .claude/rules/*
woody_frontend/ CLAUDE.md  README.md   DEPLOY.md
```

**There is no workspace-root git repository.** Each of the four modules is its own repo, checked out side by side; nothing versions the set of them together. That is why this spec lives inside `mebellar_app` rather than above it, and it is the standing reason behind §8 Phase 2's *single source-controlled workspace* item.

- **Master spec (this file)** = the "what & why" across the whole platform.
- **Per-repo `*_tz.md`** = component-level deep dives.
- **Per-repo `CLAUDE.md` (+ `.claude/rules/`)** = operational brains for day-to-day development and code review. `woody_admin/CLAUDE.md` `@`-imports `AGENTS.md` (load-bearing — do not delete).

### Cross-repo contracts that must change in lockstep

These are hand-mirrored across repos; a change in one **requires** the same change in the others (candidates for the CI parity job in Phase 2).

> **Parity checked 2026-09-18 (v1.6).** The three named status enums — `OrderStatus`, `VerificationStatus`, `ProductStatus` — were compared value-for-value across `app/domain/enums.py`, the Dart enums, and `lib/enums.ts`: **no divergence.** `MODERATOR_SCOPES` also matches between `rbac.py` and `lib/auth/roles.ts` (nine entries, `achievements` included) — though the *prose* in both repos' `CLAUDE.md` listed only eight and was corrected in the same pass. The rows added below were not previously mirrored anywhere and are listed so the next parity job knows to cover them.

| Contract | Source of truth | Mirrors |
|---|---|---|
| Status enums (`OrderStatus`, `VerificationStatus`, `ProductStatus`) | `woody_backend/app/domain/enums.py` | `mebellar_app` Dart enums, `woody_admin/lib/enums.ts` |
| **`ArStatus`** — `none\|processing\|pending_review\|approved\|rejected\|failed` (product-level cache) | `woody_backend/app/domain/enums.py` | `mebellar_app` AR models, `woody_admin` AR QC UI |
| **Per-part AR status** — `none\|pending\|processing\|approved\|rejected\|failed` (`product_ar_parts`, the canonical one) | `product_ar_parts_ar_status_check` (migration 0071) | `mebellar_app` `ArPart`, `woody_admin` `/ar-requests` queue |
| **`ArRejectCode`** — `BLURRY_PHOTOS\|COMPLEX_BACKGROUND\|POOR_LIGHTING\|INCOMPLETE_ANGLES\|OBJECT_NOT_DETECTED\|LOW_QUALITY_MODEL`; templates are **Uzbek-only** by design | `woody_backend/app/domain/enums.py` | `woody_admin` reject-reason picker |
| **Achievement `requirement_type`** — `orders_delivered\|products_active\|revenue_realized` | `achievements_requirement_type_check` (migration 0015) | `woody_admin` `/achievements`, `mebellar_app` seller badges |
| **`NotificationCategory`** — `ORDER\|SYSTEM\|PROMO\|CHAT` (the coarse generated column; the fine `notifications.type` CHECK is much larger) | `woody_backend/app/domain/enums.py` | `mebellar_app` inbox filter, `woody_admin` broadcast composer |
| **`KNOWN_PLAN_CODES`** — `free\|trial\|basic\|pro\|enterprise`; `trial` is backend-only (auto-granted, never sold) | `woody_backend/app/services/tariffs_seed.py` | `woody_admin/lib/enums.ts`, `mebellar_app` tariff screen |
| RBAC roles + moderator scopes | `woody_backend/app/security/rbac.py` | `woody_admin/lib/auth/roles.ts` |
| OTP SMS templates + SMS-Retriever signature hash (`mH1HhnJpGqi`) | Eskiz-approved templates ↔ `Settings.ESKIZ_MESSAGE_TEMPLATE_*` | `mebellar_app` `AuthSheetController._playAppSignatureHash` |

### Conventions

- **Money** is integer so'm throughout. **Timestamps** are `TIMESTAMPTZ`. Status columns are `text` + `CHECK`, never DB enums.
- **`Accept-Language`** (`uz`/`ru`/`en`, default `uz`) drives all dynamic-content localization; an unsupported value silently falls back, never 4xxs.
- **Auth** is phone + OTP → own HS256 JWT (access + refresh); the effective role is recomputed every request, never trusted from the JWT claim.

> **Note (v1.6):** this file is versioned inside `mebellar_app`, so a platform-level spec change rides that repo's history. Until §8 Phase 2's single source-controlled workspace exists, that is the only place cross-repo contract changes get reviewed at all — worth remembering when changing one of the mirrors below.
